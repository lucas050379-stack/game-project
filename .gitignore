# 빌드 산출물.
# Godot 판 exe 는 엔진이 통째로 들어 있어 100MiB 를 넘습니다(GitHub 파일 상한).
# 각 게임 폴더에서 build.bat 을 돌리면 그대로 다시 만들어집니다.
dist/

# Godot 임포트 캐시. build.bat 의 --import 가 다시 만듭니다.
.godot/

# Claude Code 로컬 설정 (사람마다 다릅니다)
.claude/settings.local.json
