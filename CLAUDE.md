# CLAUDE.md

## 이 저장소

여러 게임을 모아둔 상위 폴더입니다. **게임 하나 = 하위 폴더 하나**이고, 게임끼리는 독립적입니다.
루트에는 공용 코드나 공용 빌드가 없습니다. 게임 목록은 [README.md](README.md) 참고.

현재:
- [segyemabyul/](segyemabyul/) — 세계마뷸, C# WinForms
- [pachinko/](pachinko/) — 빠칭코 · 불멸의 이순신, C# WinForms (동작하는 버전)
- [pachinko-godot/](pachinko-godot/) — 위 게임의 Godot 4 이식, **작업 중**.
  완성되면 `pachinko/` 를 대체합니다. 그때까지 두 폴더가 같이 있는 게 정상입니다.
- [zombie-defense/](zombie-defense/) — 좀비디펜스, Godot 4 (GDScript). 뱀서라이크 생존 액션.
- [galuge/](galuge/) — 갈루가, Godot 4 (GDScript). 1945 계열 종스크롤 슈팅.
- [dragon-dash/](dragon-dash/) — 드래곤 대시, Godot 4 (GDScript).
  **이 저장소에서 유일한 안드로이드(APK) 게임**입니다. 세로 화면 · 터치 드래그.
- [beatfall/](beatfall/) — 비트폴, Godot 4 (GDScript). 세로 낙하형 4·6키 리듬게임.
  **mp4 를 넣으면 오디오를 분석해 채보를 자동으로 만듭니다.** 노트를 치면 그 자리의
  음이 나고(BMS 식 키음), 놓치면 그 음이 빕니다.
- [proyamae/](proyamae/) — 프야매, Godot 4 (GDScript). KBO 야구 매니저, **작업 중**.
  **KBO 공식 기록실에서 2000~2026 실제 기록을 긁어 카드 6스텟으로 변환합니다.**
  선수를 조작하지 않고 오더만 짜며, 경기는 자동으로 굴러갑니다.

## 작업 범위

- 작업은 **한 게임 폴더 안에서** 끝냅니다. 다른 게임 폴더는 요청받지 않는 한 건드리지 않습니다.
- 새 게임은 루트 바로 아래 새 폴더로 만들고, 그 안에 `README.md` + `src/` + 빌드 스크립트를 갖춥니다.
  다 만들면 루트 [README.md](README.md)의 게임 목록 표에 한 줄 추가합니다.
- 게임 사이에 코드를 공유하거나 공용 라이브러리 폴더를 만들지 않습니다.
  중복이 생기더라도 **폴더 하나만 복사하면 그 게임이 동작하는 상태**를 우선합니다.
- **리소스 파일을 써도 됩니다.** 예전에는 "외부 리소스 0개 · 배포는 단일 exe" 가 저장소 전체
  규칙이었지만 없앴습니다. 이미지·폰트·음원·데이터 파일을 자유롭게 두고, 런타임에 폴더를
  읽어도 됩니다. 다만 **그 게임 폴더 안에서 자급자족**해야 한다는 규칙은 그대로입니다 —
  폴더를 통째로 복사하면 리소스까지 따라와서 그대로 돌아야 합니다.

## segyemabyul (세계마뷸)

### 빌드

```
cd segyemabyul
build.bat
```

Windows 내장 C# 컴파일러(`%WINDIR%\Microsoft.NET\Framework64\v4.0.30319\csc.exe`)를 직접 호출합니다.
Visual Studio·.NET SDK·NuGet·csproj 없습니다. `src\*.cs` 전체를 한 번에 컴파일해
`dist\segyemabyul.exe` 하나를 만듭니다.

빌드 실패 시 `build.bat`은 `pause`로 멈춥니다. 비대화식으로 돌릴 때는 출력만 확인하세요.

### 지켜야 할 제약

- **그림은 코드로 그립니다.** 보드·국기·주사위·건물·캐릭터는 전부 벡터 드로잉입니다([src/Icons.cs](segyemabyul/src/Icons.cs)).
  이미지를 섞어 써도 되지만, 지금 화면이 전부 이 화풍이라 한두 개만 사진처럼 들어오면 튑니다.
- **참조는 3개뿐** — `System.dll`, `System.Drawing.dll`, `System.Windows.Forms.dll`.
  새 참조나 패키지가 필요한 방식은 쓰지 않습니다.
- **.NET Framework 4.x / C# 5 수준 문법**으로 씁니다. `csc.exe` v4.0.30319는 최신 C# 문법을 모릅니다.
  (문자열 보간 `$""`, `nameof`, 식 본문 멤버, 패턴 매칭, `async` 스트림 등 사용 불가)
- 파일을 새로 추가해도 `build.bat`은 수정할 필요 없습니다(`src\*.cs` 와일드카드).
- **내 말과 내 땅은 AI 것보다 한 단계 세게 그립니다.** 칸이 32개라 색조만으로는 묻힙니다.
  소유 표시는 `Render.DrawClaimBar` 가 칸 **바깥쪽**(판 여백)에 띠로 그립니다 —
  칸 안에 그리면 국기·이름·통행료를 가립니다. 사람 판별은 `Player.IsAI` 입니다.
  내 말은 **크기로만** 구분합니다(AI 0.186 · 사람 0.215 × `CellSize`).
- 말 둘레 표시는 `Icons.SunRays` **뿐**입니다. 원반·원형 테두리를 같이 두지 마세요 —
  가시와 따로 놀아 이질감이 생깁니다(넣었다가 되돌렸습니다). 가시를 진하게 뽑아 그 몫까지 맡깁니다.
  불꽃 색은 **플레이어 색**입니다. 흰색으로 두면 흰 칸 위에서 대비가 없습니다.
- **안쪽 반지름과 가시 끝을 번갈아 찍어** 각진 별을 만드세요 —
  매끄러운 파형으로 둘레를 흔들면 진폭을 키워도 "울렁이는 원"이지 가시로 안 보입니다.
  반지름 함수로 만든 도형은 항상 별 모양이라 `FillPolygon` 이 삼각분할에 실패하지 않습니다.
- 명멸은 **0 까지 내리지 마세요.** 완전히 꺼지면 깜빡이는 것처럼 보입니다.
  최대치의 0.5~1.0 사이(`0.75 + 0.25 * sin`)만 오르내리게 합니다.
- **`Rnd` 의 시드는 고정하지 마세요.** 고정 시드면 매 판 주사위·황금열쇠·통행료 2배 지역이
  똑같이 나옵니다(실제로 그랬습니다). `Guid.NewGuid().GetHashCode()` 로 뽑습니다 —
  `Environment.TickCount` 는 빠르게 다시 켰을 때 겹칩니다.
- **말은 4명이 서로 다른 캐릭터입니다**(`Icons.DrawHeadgear`). 얼굴 번호는 슬롯이 아니라
  `Game.AssignFaces` 가 `IsAI` 로 정합니다 — 슬롯으로 정하면 셋업에서 사람/AI 를 바꿨을 때
  내 말이 AI 얼굴이 됩니다. 시작 화면 미리보기(`Render.DrawSetup`)도 같은 규칙을 따라야 합니다.
- **랜드마크는 도시마다 도안이 다릅니다**(`LandmarkKind` · `Icons.DrawLandmark` · `Board.Landmarks`).
  새 도시를 넣으면 `Landmarks` 사전에 한 줄, `LandmarkName` 에 한 줄, `DrawLandmark` 에 case
  하나를 같이 추가하세요. 빠뜨리면 조용히 기본 첨탑(`Generic`)으로 그려집니다.
  도안은 소유자 색(`body`/`dark`)으로 칠해야 누구 것인지 읽힙니다.
- 랜드마크 건설 경로는 **두 개**입니다 — 호텔 도시 재방문(`LandmarkPhase`)과
  **출발 칸 정확히 도착**(`StartLandmarkPhase`). 후자는 목록에서 고르는 방식이라
  `PickCell`(마우스 전용) 대신 `AskList` 를 씁니다. 키보드만으로도 끝까지 진행돼야 합니다.

### 소스 구조

| 파일 | 역할 |
|---|---|
| [src/Program.cs](segyemabyul/src/Program.cs) | 진입점 |
| [src/Data.cs](segyemabyul/src/Data.cs) | 보드 32칸 · 규칙 상수 · 황금열쇠 카드 |
| [src/Game.cs](segyemabyul/src/Game.cs) | 게임 상태와 규칙 계산 (통행료, 독점, 자산, 파산) |
| [src/Ai.cs](segyemabyul/src/Ai.cs) | AI 판단 (매입 · 건설 · 인수 · 우주여행 목적지 · 매각) |
| [src/GameForm.cs](segyemabyul/src/GameForm.cs) | 60fps 루프, 트윈 엔진, 턴 진행 흐름, 입력 |
| [src/Render.cs](segyemabyul/src/Render.cs) | 레이아웃과 화면 렌더링 |
| [src/Effects.cs](segyemabyul/src/Effects.cs) | 파티클 · 코인 이동 · 플로팅 텍스트 · 화면 흔들림 |
| [src/Icons.cs](segyemabyul/src/Icons.cs) | 국기 · 주사위 · 건물 · 캐릭터 · 특수칸 아이콘 벡터 드로잉 |
| [src/Util.cs](segyemabyul/src/Util.cs) | 이징 · 색상 · 도형/텍스트 헬퍼 |

게임 규칙(건설 5단계, 통행료 2배 지역, 인수, 우주여행, 황금열쇠 등)은
[segyemabyul/README.md](segyemabyul/README.md)에 정리되어 있습니다. **규칙을 바꾸면 그 README도 같이 고칩니다.**

### 테스트

자동화된 테스트가 없습니다. 검증은 빌드 성공 + `dist\segyemabyul.exe` 실행으로 합니다.

## pachinko (빠칭코 · 불멸의 이순신)

### 빌드

```
cd pachinko
build.bat
```

segyemabyul 과 같은 방식입니다. Windows 내장 `csc.exe` 로 `src\*.cs` 를 한 번에 컴파일해
`dist\pachinko.exe` 하나를 만듭니다. 참조는 `System.dll`, `System.Core.dll`,
`System.Drawing.dll`, `System.Windows.Forms.dll` 네 개뿐입니다.

### 지켜야 할 제약

- **음악과 효과음은 코드로 합성합니다** — `winmm.dll` 의 `waveOut` 을 직접 열어 전용 스레드에서
  PCM 을 채웁니다([src/Audio.cs](pachinko/src/Audio.cs)). 음원 파일을 붙여도 되지만, 지금 소리가
  전부 이 경로로 나오므로 섞으면 볼륨과 지연을 따로 맞춰야 합니다.
- 그림은 기본적으로 코드로 그리되([src/Icons.cs](pachinko/src/Icons.cs)),
  `pachinko\art\*.png` 는 `build.bat` 이 `/resource:` 로 exe 안에 박아 넣습니다.
  `Sprites.Get(이름)` 이 있으면 그림을, 없으면 벡터를 씁니다.
- **.NET Framework 4.x / C# 5 수준 문법**만 씁니다 (segyemabyul 과 동일한 제약).
  로컬 함수·문자열 보간·식 본문 멤버 등은 컴파일되지 않습니다.
- **욱일기·일장기를 연상시키는 도안을 그리지 마세요.** 이순신 해전이 소재라 특히 문제가 됩니다.
  붉은 원반에서 방사선이 뻗는 배경, 흰 바탕에 붉은 원이 든 돛 같은 것은 쓰지 않습니다.
  화면을 집중시킬 때는 [src/Util.cs](pachinko/src/Util.cs)의 `Draw.Vignette` · `Draw.Sweep` · `GlowDot` 을 씁니다.
- 첫 `OnPaint` 가 첫 타이머 tick 보다 먼저 올 수 있습니다. `Lay` 를 생성자에서 한 번 계산해 두고,
  아이콘 그리기 함수는 크기가 0 이하일 때 그냥 반환합니다.
- `build.bat` 은 여러 줄 괄호 블록(`if (` … `)`)을 쓰므로 **반드시 CRLF 줄바꿈**으로 저장해야 합니다.
  LF 로 저장하면 `cmd` 가 블록을 잘못 읽어 엉뚱한 오류를 냅니다. 내용은 ASCII 로만 씁니다.

### 화면 구조

실제 빠칭코 기계를 본뜬 캐비닛입니다. 배치는 전부 [src/Render.cs](pachinko/src/Render.cs)의 `Lay.Compute` 에서 계산합니다.

```
캐비닛 프레임 (LED 러너)
  ├ Marquee   상단 간판 (타이틀 + MAX 배당)
  ├ Meter     해전 4칸 캡슐 램프 (칸마다 전반·중반·종반 pip 3개)
  ├ Gauge     좌측 패널 = Char(캐릭터) + GaugeBar(전의)
  ├ Mid       좌우 패널 사이 전체 영역 — 타원 링과 모서리 문양의 기준
  ├ Board/Win 릴 판 — 화면 정중앙
  ├ Side      우측 패널 (획득 LCD + 해전 기록 + 통계)
  └ Bar       조작대 + Dial(원형 핸들 = 스핀)
```

**좌우 패널(`Gauge`·`Side`)의 너비는 반드시 같아야 합니다.** 달라지면 릴 판이 중앙에서 밀립니다.
새 UI 를 넣을 때도 이 대칭을 깨지 마세요.

네모 카드가 늘어선 인상을 피하려고 릴 둘레에 **타원 크롬 링**(`DrawShroud`)을 두르고
링 바깥 네 모서리를 **단청 색동 호 + 연화문**(`DrawCorner`)으로 채웠습니다.
링 크기는 `Mid` 안에 들어가도록 잘라야 합니다 — 5×3 격자는 가로로 길어서
격자를 다 품는 타원은 화면을 벗어납니다. 링은 릴 창보다 **먼저** 그려 창 뒤로 지나가게 둡니다.

### 그림 그릴 때

셀 셰이딩으로 통일되어 있습니다. [src/Sprites.cs](pachinko/src/Sprites.cs) 의 `Art` 를 쓰세요 —
`Art.Body` 가 바탕 → 빛 반대쪽 그림자 → 광택 → 외곽선을 한 번에 처리합니다.
외곽선은 검정이 아니라 `Art.Line`(짙은 남보라)입니다. 회전 중에는 `Ico.Fast` 가 켜져
그림자와 광택을 건너뛰므로, 새 그림도 `Fast` 를 존중해야 프레임이 유지됩니다.

여러 호(`AddArc`)를 이어 붙여 태극 같은 도형을 만들지 마세요 — 모양이 쉽게 깨집니다.
`Ico.Taegeuk` 처럼 **원과 반원을 겹쳐 칠하는 방식**이 훨씬 안전합니다.

**좌표에 NaN·무한대가 들어가면 GDI+ 가 통째로 죽습니다.** 변환을 건 뒤 `g.Save()` 를 부르면
"A generic error occurred in GDI+" 로 터지는데 원인이 한참 떨어진 곳에 있어 찾기 어렵습니다.
`Math.Pow(음수, 소수)` 가 대표적인 발생원이니, 깊이·비율 같은 값은 쓰기 전에 `Ease.Clamp` 로 묶으세요.

해전 컷신([src/SeaBattle.cs](pachinko/src/SeaBattle.cs))은 **이순신이 화면 한가운데**,
왜구는 수평선에서 정면으로 다가오는 구도입니다. 적 위치는 화면 좌표가 아니라
깊이 `Z`(1 = 수평선, 0 = 코앞)와 좌우 치우침 `Lx` 로 잡고 `Project` 가 좌표·크기를 계산합니다.
그릴 때는 먼 것부터(`Z` 내림차순) 정렬해야 앞뒤가 맞습니다.

### 확률과 배당을 건드릴 때

수치는 전부 [src/Data.cs](pachinko/src/Data.cs) 상단에 모여 있습니다.
`Cumulative`(해전 단계별 누적 배당) · `Promote` · `StartTierWeights` · `ScatterPay` ·
`SpiritMax` · `JackpotBase` · `JackpotGain` 중 하나라도 바꾸면 환수율이 크게 흔들립니다.

해전은 **4개 × 전반·중반·종반 = 12단계**입니다. `Grp`(해전 4개)와 `B`(12단계)를 나눠 두었고,
`B` 는 `static D()` 에서 `PhaseShips` · `Cumulative` 로 만들어집니다. UI 는 대부분 `Grp` 기준 4칸으로 보여주고
각 칸 안에 3개의 pip 으로 단계를 표시합니다.

현재 기준값: **환수율 약 97%** (라인 26% · 총통 9% · 해전 62%), **해전 주기 약 45스핀**,
도달 해전 분포 옥포 46% · 한산도 33% · 명량 15% · 노량 6% (최종 단계 노량 종반은 해전 50번에 1번).

바꾼 뒤에는 반드시 시뮬레이션으로 확인하세요. `Program.cs` 를 뺀 `src\*.cs` 에
`Main` 을 가진 테스트 파일을 하나 붙여 `csc /main:` 으로 콘솔 exe 를 만들면 됩니다.
특히 **릴 띠마다 총통이 반드시 `ScatPerStrip` 개 들어가는지** 확인해야 합니다.
총통이 없는 릴이 하나라도 생기면 해전 확정 스핀이 총통 2개로 끝나 잭팟이 발동하지 않습니다.

### 테스트

자동화된 테스트가 없습니다. 검증은 빌드 성공 + `dist\pachinko.exe` 실행으로 합니다.
확률·배당을 바꿨다면 위 시뮬레이션을 함께 돌립니다.

## pachinko-godot (Godot 4 이식, 작업 중)

같은 게임을 Godot 4 로 옮기는 중입니다. **GDScript** 를 씁니다 —
Godot 의 C#(.NET) 빌드는 exe 옆에 .NET 어셈블리가 붙어 단일 파일이 깨지기 때문입니다.

```
cd pachinko-godot
build.bat     내보내기 (dist\pachinko.exe 한 파일)
sim.bat       헤드리스 확률 시뮬레이션 50만 스핀
```

- 엔진은 `C:\tools\godot\Godot_v4.7.1-stable_win64_console.exe` 를 기본으로 찾고,
  `GODOT_BIN` 환경 변수로 바꿀 수 있습니다. 내보내기 템플릿 4.7.1 이 필요합니다.
- `export_presets.cfg` 의 `binary_format/embed_pck=true` 면 exe 한 파일로 나옵니다.
  반드시 그래야 하는 건 아니니, 옆에 파일을 두고 싶으면 꺼도 됩니다.
- 발광은 `main.tscn` 의 `WorldEnvironment` 와 `project.godot` 의 `viewport/hdr_2d=true` 로 처리합니다.
  **색 값이 1.0 을 넘으면 번집니다.** GDI+ 판처럼 원을 여러 겹 겹치지 마세요.
- `src/data.gd` 의 `STRIP_DATA` 는 **난수로 만들지 않은 확정 배열**입니다.
  C# 판의 띠를 그대로 옮긴 값이고, 이게 곧 확률 사양입니다.
  언어마다 난수 발생기가 달라 재생성하면 환수율이 통째로 바뀝니다(실제로 26%→20% 로 떨어졌습니다).
- 로직(`data.gd`·`game.gd`)은 C# 판과 수치가 일치하는 것을 시뮬레이션으로 확인했습니다.
  한쪽을 고치면 다른 쪽도 같이 고치고, 양쪽 시뮬레이션 결과를 비교하세요.
  **단 배팅 단계와 상한은 Godot 판에만 있습니다** — C# 판은 아직 `[1,2,5,10,20,50]` 6단계입니다.
- 배팅은 `D.BET_LEVELS` 16단계(1-2-5 로 100000 까지)이고, **한 스핀 총배팅은 보유 코인의
  1/`D.BET_CAP_DIV`(=10)를 넘지 못합니다.** 상한 계산은 `Game.bet_cap()` · `_cap_idx()` 하나로
  모으고, 코인이 줄면 `clamp_bet()` 이 자동으로 내려주며 `bet_lowered` 로 화면에 알립니다.
  해전이 걸린 스핀은 배당이 아직 안 들어왔으므로 `end_battle()` 뒤에 내려야 합니다.

- **기계 앞면은 화면을 가로지르는 큰 타원**입니다(`Lay.oval_c` · `oval_r`). 좌우로는 화면 밖까지
  뻗고 위아래 호만 보여 네 모서리가 잘립니다. 위아래 줄(마키·램프 띠·조작대)의 가로 폭은
  `Lay.half_width(y)` 로 타원 안쪽에 맞추고, 그 안의 요소는 `Lay.ui_k` 비율로 함께 줄입니다.
  고정 크기를 쓰면 좁은 창에서 겹칩니다.
- **`_draw()` 에는 클리핑이 없습니다.** 릴처럼 잘라내야 하는 것은
  `clip_contents = true` 인 `Control` 자식 안에서 그립니다(`main.gd` 의 `reel_layer`).
  자식은 부모보다 뒤에 그려지니, 창을 띄울 때는 그 레이어를 숨겨야 가려지지 않습니다.
- **`floor()` 는 Variant 를 돌려줍니다** — `var x := floor(y)` 는 타입 추론에 실패합니다.
  `floorf()` / `floori()` 를 쓰세요.
- 테두리를 만들 때 `Geometry2D.clip_polygons` 로 사각형에서 안쪽을 빼려 하지 마세요.
  **굵은 선으로 두르는 쪽**이 확실합니다. 셀 셰이딩 그림자는 `intersect_polygons` 를 씁니다.
- **부드러운 빛은 반투명 원을 겹쳐 만들지 마세요** — 층 경계가 띠로 보입니다.
  `G2.glow()` 처럼 방사형 그라데이션 텍스처를 늘려 그립니다.
- **사라지는 빛은 알파로 지웁니다.** `P.hdr(색, 작은값)` 은 색만 검게 만들고 알파는 1 로 남겨
  검은 자국을 남깁니다. 밝기는 `hdr`, 페이드는 `P.a` 로 나누세요.
- **캐릭터·심볼 도형은 `G2.body()`(→ `fill_fan()`)로 칠합니다.** `draw_colored_polygon` 은
  삼각분할에 실패하면 조용히 도형을 안 그리고 stderr 에만 오류를 뱉습니다
  (`Invalid polygon data, triangulation failed`). `fill_fan()` 은 중심에서 부채꼴 삼각형을
  직접 만들어 넘기므로 실패하지 않습니다. 새 도형을 넣었으면 실행해서 **stderr 에 오류가
  없는지 반드시 확인하세요.** 흔한 원인은 ① 호 끝점을 한 번 더 넣은 중복 정점,
  ② 곡선의 자기 교차(그래서 `_catmull` 이 구심 방식입니다), ③ 화살촉처럼 안쪽이 파인
  모양에 `G2.blob()` 을 먹인 것 — 이런 건 직선 다각형으로 두세요.

### 소리 (pachinko-godot)

음원 파일 없이 `AudioStreamGenerator` 에 PCM 을 직접 밀어 넣어 전부 실시간 합성합니다
([src/snd.gd](pachinko-godot/src/snd.gd)). **오토로드**라 어디서든 `Snd.slash(3)` 처럼 부릅니다.

릴 소리는 음정을 가집니다. `reel_tick()` 이 5음계를 굴리되 릴이 하나씩 설 때마다 한 옥타브
올라가고, `reel_stop()` 의 종소리가 릴 순서대로 `STOP_CHORD`(도–미–솔–시–레)를 따라가
5개가 다 서면 화음이 완성됩니다. **딸깍 소리만 반복하면 "똑딱딱" 하고 끝나 버립니다.**

### 해전 컷신

[src/battle.gd](pachinko-godot/src/battle.gd). 이순신이 화면 한가운데,
왜구는 수평선에서 정면으로 옵니다. 적 위치는 깊이 `z`(1 = 수평선, 0 = 코앞)와
좌우 치우침 `lx` 로 잡고 `_project()` 가 좌표를 냅니다. 그릴 때는 먼 것부터 정렬해야 합니다.
**`z` 를 0..1 로 묶지 않으면 `pow(음수, 1.55)` 가 NaN 을 내고 변환 행렬이 깨집니다.**

- **칼은 올려벱니다**(`ha` 가 1.15 → -2.15). 내려베면 화면 아래 조작대가 칼끝을 가려
  마무리가 안 보입니다. y 가 아래로 크는 좌표라 각도가 줄수록 위로 돕니다 —
  잔상 방향도 같이 뒤집어야 합니다.
- **쉴 때 자세(`REST_SWING`)를 따로 둡니다.** 감긴 자세로 쉬면 칼끝이 조작대에 파묻히고,
  마무리 자세로 쉬면 칼이 머리를 가립니다. 새 베기는 지금 자세(`_swing_from`)에서
  감기 시작해야 자세가 안 튑니다.
- **베기 판정은 `_swing` 이 0 을 넘는 순간**(호의 한가운데)입니다. 고정 비율로 잡으면
  이징을 바꿀 때마다 싱크가 깨집니다. 동작이 끝난 뒤에 죽이면 적이 지나간 다음 칼이 나갑니다.
- **`STRIKE_Z` 안에 든 적이 있으면 리듬을 무시하고 즉시 휘두르고, 그 적은 전부 벱니다.**
  타이머로만 돌리거나 개수로만 세면 코앞의 적을 놔둔 채 칼이 헛돕니다.
- **칼은 양손으로 잡습니다.** `Art.admiral_full` 은 자루 위치(`grip`)를 먼저 정하고 양팔을
  거기에 맞춰 그립니다. 잔상(`trail_amt`)은 휘두르는 중에만 0보다 커야 합니다 —
  안 그러면 가만히 서 있는데 잔상이 부챗살로 남습니다.
- 해전 중 연타(`mash_hit()`)에 **상한은 없습니다.** 게이지가 `MASH_LAP`(100회)마다 한 바퀴
  돌 뿐입니다. 보너스는 `연타수 × 베팅 × 0.04` 로 `BattleRun.bonus` 에 쌓였다가
  `end_battle()` 에서 함께 지급됩니다. **시뮬레이션에는 안 잡히고 위쪽이 열려 있으므로**
  환수율 목표를 잡을 때 따로 감안하세요(빠른 손이면 베팅의 4~8배).
- 승급 확률(`D.PROMOTE`)과 배당표(`D.CUMULATIVE`)는 **한 쌍으로 움직입니다.** 승급만 올리면
  환수율이 곧바로 100% 를 넘습니다. 둘 중 하나를 건드렸으면 `sim.bat` 을 돌리세요.
- **전의 게이지는 스핀 수로만 찹니다**(`D.SPIRIT_PER_SPIN` = 1, `D.SPIRIT_MAX` = 110스핀).
  배팅액으로 채우면 배팅을 올릴수록 해전이 빨라져 **환수율 자체가 배팅에 따라 달라집니다**
  (최대 배팅 환수율이 183% 까지 갔던 적이 있습니다). 여기에 배팅액을 다시 넣지 마세요.
- **해전 빈도(`SPIRIT_MAX` · `JACKPOT_BASE`)와 배당(`CUMULATIVE`)은 한 덩어리입니다.**
  빈도만 올리면 환수율이 곧바로 100% 를 넘습니다 — `SPIRIT_MAX` 를 133→110 으로 줄였을 때
  99%, `JACKPOT_BASE` 를 0.35%→0.85% 로 올렸을 때 102% 가 나와 매번 배당표를 낮춰 되돌렸습니다.
  해전이 잦아지면 **총통 환수율도 같이 오릅니다**(확정 스핀은 총통 3개 이상을 띄우므로).
  `JACKPOT_BASE` 는 전의 게이지 아래에 그대로 표시되는 값이기도 합니다.

C# 판의 기능은 모두 옮겼습니다. 진행 상황은
[pachinko-godot/README.md](pachinko-godot/README.md) 표를 보세요.

## zombie-defense (좀비디펜스)

Godot 4 / GDScript. 뱀서라이크 — 이동만 하고 공격은 자동, 레벨업마다 스킬을 고르고 진화시킵니다.
한 판은 **라운드의 연속**입니다: 직업 선택 → 라운드를 버팀 → 코인으로 기본 스텟 강화 → 다음 라운드.

**직업이 셋(무사·마법사·궁수) + 해금 셋이고, 스킬은 직업마다 5종씩 15종**입니다. 직업은
이름표가 아니라 사거리·퍼짐·표적 수를 나눠 가진 세 가지 싸우는 방식입니다.

**뱀서(Vampire Survivors)의 구조를 그대로 옮겨 왔습니다** — 한 라운드(8~15분)가 뱀서의 한 판에
해당하고, 스킬 6칸 + 패시브 4칸, 스킬 6레벨, **진화는 보스가 떨군 보물상자에서** 일어나며,
아르카나 · 유니온 · 리미트 브레이크 · 행운/저주 · 리롤/스킵/밴 · 바닥 골드 · 부술 수 있는 통 ·
캐릭터 해금이 전부 있습니다. **이 게임에만 있는 것은 둘뿐입니다 — 직업별 진화(`evo_by`)와
보스 스킬.** 자세한 표는 [zombie-defense/README.md](zombie-defense/README.md) 참고.

> **뱀서에 없는 것을 새로 만들 때는 위 둘과 같은 급인지 먼저 재세요.** 고유 스킬 잠금과
> 탄 차단 스킬은 한때 있었지만 "뱀서에 맞춘다"는 방침에 따라 걷어냈습니다.

```
cd zombie-defense
build.bat     내보내기 (dist\zombie-defense.exe 한 파일)
```

엔진·내보내기·발광 처리는 pachinko-godot 과 같은 규칙입니다
(`GODOT_BIN`, `embed_pck=true`, `hdr_2d`, 색 1.0 초과 시 번짐).

### 지켜야 할 제약

- **그림은 `art/*.png` 가 있으면 그걸 쓰고 없으면 벡터로 그립니다**(`Spr.get_tex`).
  `art/` 는 비어 있어도 게임이 돌아야 합니다 — 로더는 `ResourceLoader.exists` 로 먼저 확인합니다.
  PNG 는 빌드 때 PCK 로 묶입니다.
  필요한 그림 목록과 이미지 AI 프롬프트는 [art/README.md](zombie-defense/art/README.md) 에 있습니다.
- **걷는 캐릭터(주인공 + 좀비 6종)는 전신 한 장이 아니라 부위별 컷아웃**(`_torso`·`_head`·
  `_arm_b`·`_arm_f`·`_leg_b`·`_leg_f`)입니다. `Art._cutout_body()` 가 벡터 그림과 똑같은
  관절 좌표(pivot·tip)를 받아 `Spr.limb_tex()` 로 팔다리를 매 프레임 실제로 회전시켜 그립니다
  — 정지 프레임을 갈아 끼우는 방식이 아니라서 몇 장을 넣든 걸음이 항상 매끄럽습니다. 주인공도
  화면에 하나뿐이라 Godot `Skeleton2D` 를 써도 성능엔 문제없지만, 노드를 하나도 안 쓰는 이
  프로젝트 구조에 맞춰 좀비와 같은 코드 경로(관절 회전)를 씁니다. `_torso` 가 없으면 그 캐릭터는
  통째로 벡터로 돌아가고, 다른 부위가 빠지면 그 부위만 안 보입니다. `gem.png`·`drone.png` 는
  걷지 않으므로 전신 한 장짜리 예전 방식 그대로입니다.
- **이미지 AI 는 "투명 배경"을 달라고 해도 회색 체크무늬를 그림으로 그려 놓는 일이 잦습니다.**
  알파는 전부 255 라 그대로 쓰면 사각형이 보입니다. 가장자리에서 flood fill 로 지우고,
  남은 밝은 테두리를 깎고, 팔 안쪽처럼 **갇힌 배경**은 순회색 덩어리 크기로 따로 걸러야 합니다.
  원본 시트는 `art/source/` 에 두고 그 폴더에 `.gdignore` 를 넣어 exe 에서 뺍니다 —
  안 넣으면 참조용 시트가 그대로 PCK 에 딸려 들어갑니다.
- **`<이름>_torso.png` 에 팔이 같이 그려져 있으면 안 됩니다.** 프롬프트에 "torso only, no arms"
  를 넣어도 이미지 AI 가 어깨~팔을 같이 그려 오고(좀비류 6종이 전부 그랬습니다), 그대로 쓰면
  `_arm_b`/`_arm_f` 가 그 위에 또 얹혀 **팔이 두 벌** 보입니다. 팔 조각을 포기하고 몸통째로
  기울여 쓰는 우회는 걷기에 맞춰 팔이 안 움직이고 원본 시트와도 달라 보여 되돌렸습니다.
  `art/source/dearm.ps1` 이 외곽선을 벽 삼아 영역을 나눠 팔만 도려냅니다 — 세로선으로 자르는
  게 아니라 원래 테두리를 따라 잘립니다. 원본은 `art/source/torso_original/` 에 남겨 둡니다.
  **팔을 지운 뒤에는 반드시 내용 경계로 다시 잘라야 합니다**(`dearm.ps1` 이 같이 합니다).
  `Spr.blit` 은 캔버스 기준으로 가운데를 잡으므로, 팔이 있던 자리가 여백으로 남으면 몸이
  한쪽으로 밀립니다(좀비 10px, 폭탄 좀비 15px 씩 밀려 있었습니다).
- **팔다리 관절 좌표는 벡터 그림 것을 그대로 쓰면 안 됩니다.** 벡터 몸통은 폭이 `r` 안팎인데
  그림 몸통은 훨씬 넓어서(비대 1.76r, 보스 1.86r), 같은 좌표를 넘기면 팔다리가 어깨·엉덩이가
  아니라 **배 한가운데에서 자란 것처럼** 보입니다. 보스 다리가 갈비뼈 밑에 뼈다귀 두 개로
  붙어 보이던 게 이것이고, 주인공은 손잡이(0.42r)·다리 끝(0.62r)이 전부 몸통 그림 안에
  파묻혀 **팔다리가 아예 안 보였습니다.** 캐릭터마다 `_cutout_body` 호출 바로 위에서
  그림 폭에 맞춘 어깨(`shb`/`shf`)와 엉덩이 간격을 따로 잡습니다.
  `Spr.limb_tex` 는 길이에 맞춰 **두께도 같이** 늘리므로, 굵게 하려면 더 길게 뻗으면 됩니다
  (보스의 비대칭 큰 팔이 이 방식입니다 — 큰 팔 그림을 따로 두지 않았습니다).
- 스프라이트를 좌우로 뒤집을 때는 `draw_set_transform` 이 **카메라 변환을 덮어씁니다.**
  `main.gd` 가 매 프레임 `Spr.origin` 에 카메라 오프셋을 넣어 주고, 뒤집기가 끝나면 되돌립니다.
- **수치는 전부 [src/data.gd](zombie-defense/src/data.gd)에 모읍니다.** 직업·스킬·적·보스 스킬·
  웨이브·경험치·아르카나 곡선이 한 파일에 있습니다. 스킬의 레벨별 값은 **길이 7짜리 배열**
  이고 마지막 칸(`EVO_LV`)이 진화 상태입니다.
- **직업 축은 `D.WEAPON` 의 두 칸이 전부입니다** — `cls`(어느 직업 것인가 · 그 직업이 들면
  피해 +`CLASS_BONUS`),
  `pair`(진화의 짝 패시브), `evo_by`(직업별 진화). 새 스킬을 넣을 때 `cls`·`pair` 를
  빠뜨리면 조용히 0번(무사 · 화약 주머니) 것이 됩니다.
- **새 화면을 만들면 `_key` 와 `_unhandled_input` 의 마우스 분기를 둘 다 채우세요.**
  키만 넣으면 키로는 되기 때문에 "마우스로 못 고른다"를 눈치채기 어렵습니다 —
  아르카나·보물상자가 실제로 그렇게 나갔습니다. 그리고 **자리 함수를 하나 두고 그리기와
  클릭 판정이 같이 보게** 하세요(`Hud.arcana_rect`). 레벨업 카드는 두 곳에서 따로 계산하는데,
  그 방식은 한쪽만 고치면 조용히 어긋납니다.
- **입력은 다음 프레임에 처리됩니다.** `Input.parse_input_event` 로 시험할 때 누른 직후에
  상태를 읽으면 안 바뀐 것처럼 보입니다 — 한 프레임 뒤에 읽으세요.
- **직업 선택창의 카드 안은 위에서 아래로 흘려 놓습니다**(`Hud.charsel`). 자리를 상수로
  박아 두면 설명 글이 창 폭에 따라 2줄이 되기도 3줄이 되기도 해서 아래 것을 덮습니다 —
  실제로 설명이 스킬 문양을 가렸습니다. `Hud._wrap` 이 **다음 줄이 시작될 y** 를 돌려주므로
  이어 그리는 쪽은 그 값을 받아 쓰세요.
- **잠긴 스킬은 없습니다.** 뱀서처럼 어느 직업이든 결국 15종을 다 주울 수 있고, `D.CHAR` 의
  `weapon` 은 그저 시작 스킬입니다. 예전에 `unique` 잠금이 있었지만 걷어냈습니다.
  해금 캐릭터(`lock`)는 `D.char_locked` 가 [Sv] 의 누적 기록으로 판정합니다.
- **캐릭터 `dmg` 배율은 셋 다 1.0 으로 둡니다.** 예전 중장병처럼 1.15 를 걸면 `CLASS_BONUS`
  와 두 겹으로 쌓여 어느 쪽이 무슨 몫인지 알 수 없게 됩니다. 직업 차이는 체력·이동 속도와
  스킬의 성격으로만 냅니다.
- **진화는 레벨업 카드가 아니라 보물상자에서 일어납니다**(`Game.open_chest`). 보스를 잡으면
  반드시 떨어지고, **유니온 → 진화 → 보통 강화** 순으로 상자 칸을 채웁니다. 그래서
  `round_bosses` 는 **1라운드에도 최소 하나**를 냅니다 — 보스가 없는 라운드는 진화가 아예
  불가능한 라운드가 되어 게임의 핵심 고리가 한 번도 안 돌아갑니다.
- **상시 전개형(`guard`·`ward`)은 `cd` 가 없습니다.** [World]._step_areas 가 장판 하나를
  띄워 두고 매 프레임 반경·피해를 갱신하고, `_fire` 는 `SELF_RUN` 에 든 kind 를 건너뜁니다.
  **`SELF_RUN` 에 빠뜨리면 `_shoot` 이 분기 없이 헛돌며 쿨다운만 돕니다**(조용히 아무 일도
  안 일어납니다). 장판은 `AREA_TICK`(0.32초)마다 `dmg × 0.32` 를 주므로 **초당 피해 = dmg** 입니다.
- **성장이 두 갈래인 걸 섞지 마세요.**
  - **판 안**(레벨업 카드) = **스킬 + 패시브 5종 + 리미트 브레이크 + 아르카나.** 패시브는 **진화의 짝**이라 있는 것이지
    수치를 올리려고 있는 것이 아닙니다 — 진화 조건이 "스킬 만렙 **+ 짝 패시브 보유**"
    둘이고, 한 판에 4종만 들 수 있어(`D.MAX_PASSIVES`) 여섯 스킬을 다 진화시킬 수는 없습니다.
    **패시브를 더 늘리지 마세요.** 예전에 "공격력 +%" 류가 카드의 절반을 먹으면서 고를 때
    아무 감흥이 없어 통째로 걷어냈던 적이 있고, 늘리는 만큼 그 문제가 그대로 돌아옵니다.
    패시브도 라운드마다 초기화됩니다.
  - **판 밖**(라운드 클리어 코인 상점, `D.SHOP` · [src/save.gd](zombie-defense/src/save.gd)) =
    **기본 스텟만.** 죽어도 남습니다. `Game.max_hp()`·`speed()`·`dmg_mult()`·`cd_mult()`·`pickup()`
    이 캐릭터 고유값 × 이 강화를 곱해 냅니다.
- **상점 만렙(100)을 바꿀 때는 레벨당 값과 비용 배수를 같이 보세요.** 만렙이 2~6이던 시절의
  값(레벨당 +12%, 비용 1.55배)을 그대로 100단계로 늘리면 체력이 13배가 되고 비용은
  `40 × 1.55^99` 로 **64비트 정수를 넘깁니다.** 지금은 "100레벨에서 도달할 지점"을 먼저 정하고
  100으로 나눈 값이고, 비용은 1.045배입니다. 상점 화면도 알갱이가 아니라 숫자+막대로 그립니다 —
  알갱이 100개는 그릴 수도 읽을 수도 없습니다.
- **저장은 `user://save.cfg` 하나**입니다(코인·강화 단계·최고 라운드).
  읽기/쓰기가 실패해도 게임은 기본값으로 돌아야 합니다.
- **라운드 수치는 `D` 의 `round_time`·`req_lv`·`power`·`tier_mix`·`round_bosses`·`round_coins`
  여섯에 모읍니다.** 라운드가 난이도를 내는 통로는 `tier_mix` 하나뿐입니다 —
  아래 "난이도는 한 줄의 식입니다" 항목을 먼저 읽으세요.
  라운드 길이는 **10라운드 단위 계단**(2분 → 5분)입니다.
- **맵 크기 · 화면 배율 · 스폰 거리는 한 덩어리입니다.** 화면에 담기는 월드는 `main.gd` 의
  `ZOOM`(= 1/1.32)이 정하고 1280×720 화면이면 1690×950, 반대각선이 969 입니다. 적은 그
  밖(`D.SPAWN_MIN` = 1020 · `D.BOSS_SPAWN`)에서 나와야 합니다 — **`ZOOM` 만 넓히면 적이
  화면 안에서 툭 튀어나옵니다.** 화면을 넓히면 그린 적 수도 넓이의 제곱만큼 늘어
  `TIER0_MAX_SHOWN` 에 더 자주 걸립니다. 셋 중 하나를 만지면 나머지 둘을 같이 보세요.
- **떨어지는 아이템(`World.items`)은 젬과 다른 배열입니다.** 자석(`D.MAGNET_CHANCE` 0.3%)과
  구급 상자(`D.HEAL_CHANCE` 0.5%) 둘입니다. **젬처럼 따라오게 만들지 마세요 — 자석에도
  안 끌려옵니다.** 젬을 끌어오는 곳은 `_step_gems` 하나뿐이고 `items` 는 아무도 건드리지
  않습니다. 주우러 가는 것이 이 아이템들의 유일한 대가이고, 자석 하나로 구급 상자까지
  딸려 오면 그 대가가 통째로 사라집니다. 대신 미니맵에 종류별 색으로 표시해 **어디 있는지는**
  알려 줍니다(맵이 넓어 화면만으로는 못 찾습니다).
- **회복량은 절대값이 아니라 최대 체력의 비율입니다**(`D.HEAL_PCT`). 캐릭터마다 최대 체력이
  86~150 이고 방탄 조끼로 4배까지 오르므로, 고정값이면 같은 상자가 누구에게는 한 방이고
  누구에게는 눈금 하나가 됩니다.
- **한 마리는 많아야 아이템 하나를 떨굽니다.** `_die` 가 난수를 **한 번만** 뽑아 누적 확률로
  가릅니다 — 종류마다 따로 뽑으면 드물게 둘이 같은 자리에 겹쳐서, 화면에서 무엇을 주웠는지
  읽을 수가 없습니다.
- **아이템은 실루엣으로 갈립니다** — 젬은 동그라미 한 겹, 자석은 말굽, 구급 상자는 네모.
  색만 다르면 젬이 수백 개 깔린 판 후반에 통째로 묻힙니다. 새 아이템도 **새로운 모양**으로
  주세요. `P.hdr` 배수는 1.35 근처까지만 — 그 위로 올리면 채널 둘이 잘려 색이 하얗게
  날아가고(옥색이 청록으로 보입니다) 색 규칙이 깨집니다. 밝기는 `G2.glow` 가 냅니다.
- **맵은 `D.MAP` 세 종을 라운드마다 돌려 씁니다**(`map_of`). 크기가 다르므로 `D.ARENA` 같은 상수는
  없고 `World.arena` 를 봅니다. **맵 밖은 `void` 색으로 칠하고 무늬를 안 그립니다** — 예전에는
  화면 전체를 같은 바닥색으로 칠해 맵 끝이 전혀 안 보였습니다. 그 위에 `Art._wall` 이 발광선 +
  빗금 + 모서리 꺾쇠를 얹습니다.
- **직업은 `D.CHAR` 에 한 줄 더하면 선택창에 바로 나옵니다.** 그림 6조각이 준비되기 전에는
  `Spr.char_base` 가 `hero` 그림으로 대신하고 `tint` 로 색만 바꿉니다. `Spr.tint` 는 전역이라
  **쓰고 나면 반드시 흰색으로 되돌려야** 다음에 그리는 적이 물들지 않습니다.
- **보스는 걷기만 하지 않습니다** — 거리에 따라 돌진·강타·산탄을 씁니다(`World._boss_ai`).
  세 가지 규칙을 지키세요.
  ① **예비 동작(`D.BOSS_CAST`) 없이 나가는 스킬을 만들지 마세요.** 즉발이면 피할 방법이
     없어서 난이도가 아니라 불합리가 됩니다. 그 동안 보스는 멈춰 서고, 멈추는 것 자체가
     플레이어에게 주는 시간입니다.
  ② **방향은 스킬을 고르는 순간 `dv` 에 박아 두고 그 뒤로 안 고칩니다.** 예비 동작 동안
     계속 겨냥을 고쳐 잡으면 경고 그림(`Art._boss_cast`)과 실제로 나가는 방향이 달라져,
     경고를 보고 피한 사람이 그대로 맞습니다. 두 함수가 **같은 `dv`** 를 봐야 합니다.
  ③ **경고 표시는 그리기 등급을 낮춰도 항상 그립니다.** 피할 수 있어야 하는 것은 프레임이
     떨어졌다고 안 보여도 되는 정보가 아닙니다. 세 스킬의 경고 모양도 서로 달라야 합니다
     (띠·원·부채꼴) — 다 같은 붉은 고리면 "뭔가 온다"까지만 알고 어디로 피할지를 모릅니다.
  강타는 **플레이어만** 다치게 하세요. 보스가 자기 무리를 갈아 버리면 판이 오히려 쉬워집니다.
  보스 탄도 `P.VENOM` · kind `"spit"` 그대로라 차단 스킬이 자동으로 지웁니다.
- **노드를 만들지 마세요.** 적·투사체·젬은 `Dictionary` 배열로 들고 `_draw()` 한 곳에서 그립니다.
  수백 개를 노드로 만들면 생성·해제 비용만으로 프레임이 무너집니다.
- **적을 죽일 때 배열에서 바로 빼지 마세요.** 충돌 판정이 매 프레임 만든 격자(`World._grid`)의
  **인덱스**로 돌기 때문에, 중간에 `remove_at` 하면 뒤쪽 인덱스가 밀려 엉뚱한 적을 때립니다.
  `dead` 표시만 남기고 프레임 끝의 `_sweep()` 이 정리합니다. 모든 판정 루프는 `dead` 를 건너뜁니다.
- **충돌은 격자로만.** 전수 비교(적 300 × 탄 200)는 GDScript 로 감당이 안 됩니다.
  `_near()` 로 주변 3×3 칸만 훑고, 적끼리 겹침 밀어내기는 자기 칸 안에서만 봅니다.
- **라운드 클리어 순간 남은 젬을 전부 흡수합니다.** 즉시 더하지 않고 `St.SWEEP` 상태에서
  **강한 자석으로 끌어오는 장면**(`World.sweep_step`)을 재생합니다 — 적은 멈추고 젬만
  점점 빨라지며 빨려 들어옵니다. `World.absorb_gems` 는 그 뒤에 남은 것을 처리하는
  **안전장치**이지 본 경로가 아닙니다(4.5초 상한에 걸리거나 젬이 화면 밖에 있을 때).
  보상 코인이 그 라운드에서 번 경험치(`Game.round_xp`)에 비례하므로 **흡수를 코인 계산보다
  먼저** 해야 마지막에 쓸어 담은 젬이 보상에 잡힙니다. `xp` 는 레벨업마다 깎이니 누적량을
  따로 셉니다. HUD 는 `St.PLAY` 와 같은 분기에서 `St.SWEEP` 을 그려야 화면이 끊기지 않습니다.
- **수집 범위 밖 젬도 천천히 따라오게** 두세요. 원거리 스킬이 붙으면 멀리서 죽인 젬이
  맵에 그대로 남아 경험치가 안 들어옵니다. **자석을 약하게 하면 젬이 쌓입니다** — 실제로
  적 590마리에 젬 589개가 깔려서 적을 한 벌 더 그리는 비용이 들었습니다. 그래서 `D.GEM_CAP`
  을 두고 넘치면 가장 오래된 것부터 그 자리에서 흡수합니다(경험치는 그대로 줍니다).
- **젬은 그리기 호출 딱 한 번(`draw_circle`)입니다.** 후광을 따로 그리거나 하이라이트를
  얹지 마세요 — 화면에 수백 개가 깔리므로 한 겹만 늘어도 비용이 두 배입니다.
  발광은 색을 1.0 위로 올려(`P.hdr`) 엔진 블룸에 맡깁니다. 그림(`gem.png`)도 이 이유로 뺐습니다.
- **적을 밀어내는(넉백) 수단을 만들지 마세요.** 대지 분쇄와 회전 사슬이 넉백을 걸었더니
  근접 적이 영영 플레이어에게 닿지 못해서 "가만히 서 있어도 아무 일도 안 일어나는" 판이
  됐습니다 — 이 게임에서 제일 불합리한 상태였습니다. 두 스킬은 지금 **피해만** 줍니다.
  **대신 반지름을 키워서 존재감을 내세요.** 넉백을 뺀 직후 둘은 사거리 82~140 / 96~210 짜리
  순수 광역 피해라 몸에 붙은 적만 스쳤습니다. 지금은 사슬 126~240 · 필드 148~330 입니다.
  회전 사슬은 **반지름과 도는 속도(`World.orb_ang`)를 같이** 만져야 훑는 넓이가 체감됩니다 —
  반지름만 키우면 궤도가 커진 만큼 한 바퀴가 느려져 제자리입니다. 다만 **사냥매가 같은
  `orb_ang` 을 봅니다.** 속도를 올렸으면 사냥매 쪽 배수(`orb_ang * 0.47`)를 같이 낮춰
  궤도 속도를 유지하세요. 안 그러면 매가 팽이처럼 돕니다.
- **진화는 만렙(6레벨)의 2.0배**입니다 — 자세한 이유는 위 난이도 항목에 있습니다. 새 스킬을
  넣거나 수치를 만졌으면 `dmg × count / cd` 를 만렙(6레벨) 칸과 나눠 2.0 근처인지 보세요.
  상시 전개형은 `cd` 가 없으므로 `dmg` 만으로 잽니다. **`evo_by`(직업별 진화)만 2.1배**이고,
  **여섯 가지가 서로 같아야 합니다** — 벌어지면 "어느 직업으로 진화시키느냐"가 곧 최적해가
  되어 선택이 사라집니다. 지금은 기본 15종이 1.98~2.02, 직업별 6가지가 2.098~2.100 입니다.
  **2.0 과 2.1 을 0.1 이상 벌리지 마세요** — 남의 스킬을 주워 진화시키는 것이 유일한
  정답이 되어 직업 고유 스킬이 밀려납니다. **5 → 6레벨로 늘릴 때는 예전 5레벨 값을 6레벨 칸에
  두고 사이에 한 칸을 끼워 넣었습니다** — 그래야 진화 칸을 다시 계산할 필요가 없습니다.
- **더 올릴 데가 없으면 리미트 브레이크입니다**(`D.LIMIT`). 뱀서와 같은 자리 — 판이 끝나기
  전에 남은 레벨업이 통째로 버려지지 않게 전체 스텟을 조금씩 올립니다. 상한이 없으므로
  카드가 마르지 않습니다(예전의 구급 상자 채우기를 대신합니다). 라운드가 끝나면 사라집니다.
- **패시브 · 아르카나 · 리미트는 `Game.pmul` 한 통로를 같이 지납니다.** 새 것을 넣을 때
  곱하기를 호출부에 흩뿌리지 마세요 — `stat` 이름만 맞추면 기본 스텟 함수들이 알아서 받습니다.
- 스킬은 **가장 가까운 적**을 겨냥합니다. 이동 방향으로만 쏘면 서 있을 때 아무것도 못 해서
  "공격은 자동"이라는 장르 규칙이 깨집니다. 겨누는 방향은 `World.look` 하나로 모아 두고
  **그림(`Art.hero`)과 발사가 같은 값을 봅니다** — 각자 가장 가까운 적을 찾으면 한 프레임
  어긋나 손에 든 것과 탄이 따로 놉니다. 손에 든 소품은 직업마다 다릅니다(`Art._prop` —
  칼·지팡이·활). 궁수의 시작 스킬은 활에서 화살이 나가야 합니다.
- 새 스킬을 넣으면 `D.WEAPON` 한 줄 + `World._shoot()` 의 `match kind` 분기 +
  `Hud.icon()` 문양을 **셋 다** 추가해야 합니다. 빠뜨리면 조용히 아무 일도 안 일어납니다.
  쿨다운으로 안 쏘는 종류(접촉·상시 전개·따라다니는 것)라면 `World.SELF_RUN` 에도 넣으세요.
- **15종이 되면서 문양이 겹치기 쉬워졌습니다.** `Hud.icon` 은 직업마다 축을 나눠 뒀습니다 —
  무사는 닫힌 도형(고리·육각·도끼), 마법사는 덩어리와 원(불덩이·룬), 궁수는 가늘고 긴 선
  (화살·시위). 새 문양은 자기 직업 쪽 축을 따르세요. 색은 `P.cls()` 가 직업 색을 냅니다.
- **둥근 것은 반드시 `draw_circle` 로 그리세요.** `G2.fill_fan`
  (= `canvas_item_add_triangle_array`)은 호출마다 그리기 명령이 따로 생겨 **배칭이 끊깁니다.**
  머리를 fill_fan 으로 그렸더니 200마리에서 23fps 였고, `draw_circle` 두 번(`Art._ball`)으로
  바꾸자 60fps 가 나왔습니다. fill_fan 은 몸통처럼 원으로 못 만드는 도형에만 씁니다.
  화면에 수십 개씩 깔리는 것(젬 등)은 도형을 겹치지 말고 한 겹으로 두세요.
- **적은 종류마다 실루엣이 달라야 합니다.** 원만 겹치면 색이 달라도 다 한 덩어리로 보입니다.
  몸통 형태·팔다리 길이·기울기를 나누고 `walk`(걷기 위상)·`face`(바라보는 쪽)를 적마다 들려 보냅니다.
- **적은 종 5 × 티어 4 의 격자입니다.** 종(`art`)이 실루엣과 행동을, 티어(`tint`)가 색과
  강함을 정합니다. `D.ENEMY` 는 **티어 우선 순서**여야 합니다 — 0~4 가 T0, 5~9 가 T1 …
  `World._roll_kind` 가 `티어 × D.SPECIES + 종` 으로 색인하므로 순서가 흐트러지면
  엉뚱한 적이 나옵니다. `E_BOSS` 는 **항상 맨 끝**(격자 밖)이고, `WAVE` 의 `w` 는
  **종 개수(5)만큼**입니다. **특수 행동은 종류 번호가 아니라 `art` 로 갈라야 합니다**
  (침 뱉기·폭발·체력 띠·그리기 분기 전부) — 번호로 비교하면 티어를 늘릴 때마다 빠뜨립니다.
- **라운드가 끝나면 스킬과 레벨이 초기화됩니다**(`Game.next_round`). 한 라운드가 곧 한 판이고
  라운드 번호는 난이도만 올립니다. **레벨과 경험치도 같이 되돌려야 합니다** — 레벨만 높은 채로
  스킬을 비우면 다음 레벨까지 필요한 경험치가 이미 커져 있어서 스킬을 다시 갖출 수가 없습니다.

### 난이도는 한 줄의 식입니다 — 여기를 건드리기 전에 반드시 읽으세요

> **라운드 n 은, 모든 강화를 `req_lv(n)` 단계까지 올린 화력이면 클리어된다**

- 이 식이 난이도의 **전부**입니다. `D.power(lv)` = 상점 공격력 × 재사용 대기 = 전 항목
  `lv`강일 때의 초당 피해 배율이고, `D.tier_mix(n)` 이 라운드 n 의 평균 체력 배율을
  **정확히 `req_power(n)`** 이 되도록 이웃한 두 티어를 섞습니다(실측 오차 0.000%).
- **라운드 길이는 등식을 건드리지 않습니다** — `hp_scale` 도 `wave_at` 도 초가 아니라
  진행도로 세므로 판이 통째로 늘어날 뿐입니다. 다만 **판 끝은 헐거워집니다**(적의 최종
  체력 배율은 그대로인데 플레이어는 레벨업 카드를 더 받습니다). 길이를 만졌으면
  레벨업 횟수를 재 보세요 — 지금은 3분 라운드에 **29번**, 7분 라운드에 **48번**이고,
  스킬 5개를 5레벨까지 올리는 데만 **25장**이 듭니다. 이 관계가 패시브를 넣을 수 있느냐를
  정합니다(2분 시절엔 22번이라 자리가 없었습니다).
- **난이도(`D.DIFFICULTY`)는 곱하기 셋입니다** — 적 체력 · 적 피해 · 코인. **모든 라운드에
  똑같이 걸리므로 등식을 건드리지 않습니다.** 라운드별로 다른 값을 주고 싶어질 때 여기를
  쓰세요. 적 피해는 `World._edmg` 한 곳에서만 곱합니다(접촉·침·보스 스킬이 전부 거기서
  나옵니다). **경험치에는 걸지 마세요** — 어려운 난이도에서 레벨업이 빨라져 빌드가 더 빨리
  완성되면 어려운 쪽이 오히려 쉬워집니다. 대가는 코인으로만 줍니다.
- **난이도와 시작 라운드는 아무것도 잠그지 않습니다.** 도달 기록(`Sv.best_round`)은
  기본값과 참고 표시일 뿐이고, 고를 수 있는 범위는 1~`D.MAX_ROUND` 입니다.
- **`req_lv(n)` 은 직선이 아닙니다.** 양 끝만 못 박혀 있고(`req_lv(1)=0` · `req_lv(100)=99`)
  그 사이가 `D.REQ_POW`(=1.6) 제곱으로 휩니다 — 아래로 볼록해서 초·중반 요구가 내려가고
  마지막 20라운드에 몰립니다. 예전에는 `n-1` 인 직선이었는데 실제로 돌려 보니 중반이
  빡빡하고 후반이 밋밋했습니다. `REQ_POW = 1.0` 이면 그 직선으로 정확히 되돌아갑니다.
- **`REQ_POW` 와 `HP_RAMP` 를 헷갈리지 마세요.** `HP_RAMP` 은 모든 라운드를 똑같이 올리고
  내리는 **판 전체의 세기**이고, `REQ_POW` 는 **라운드 사이의 기울기**만 바꿉니다.
  "전체가 헐겁다"면 `HP_RAMP`, "초반이 빡빡하고 후반이 밋밋하다"면 `REQ_POW` 입니다.
  둘 다 등식을 건드리지 않으므로 **라운드별로 손대고 싶은 충동은 여기서 해소하세요.**
- **곡선을 휘어도 접촉 피해 불변식은 저절로 따라옵니다.** "몇 대 맞으면 죽나"는
  `내 체력배율 ÷ 적 피해배율` 인데 양쪽 다 `req_lv(n)` 에서 나오므로, 비가 0.999~1.069 로
  유지됩니다 — `TIER_DMG_POW` 를 다시 구할 필요가 없습니다.
- **식이 성립하려면 라운드끼리 다른 것이 딱 하나여야 합니다.** 그래서 라운드가 바꾸는 것은
  **티어(색)뿐**입니다. 적 체력에 라운드 배율을 곱하지 말고(`round_hp` 를 지웠습니다),
  스폰 간격·수도 라운드마다 바꾸지 마세요(`round_rate`/`round_burst` 를 지웠습니다).
  `wave_at` 도 라운드 번호를 보지 않습니다. **여기에 두 번째 통로를 만드는 순간 검산이
  불가능해지고, 그때부터는 수치를 아무리 다듬어도 맞출 수 없습니다.**
- **판 안의 시간 배율(`D.hp_scale`)은 초가 아니라 진행도로 셉니다.** 라운드 길이가 2~5분으로
  다르므로 초로 세면 긴 라운드가 그 사실만으로 더 어려워져 등식이 깨집니다.
- **경험치는 티어를 타면 안 됩니다 — 종(種)에만 답니다.** 등식이 성립하면 **초당 처치
  수가 모든 라운드에서 같습니다**(화력도 적 체력도 똑같이 `req_power(n)` 배니까요). 그래서
  마리당 경험치를 티어 배율로 올리면 초당 경험치가 그대로 티어 배(100라운드 17배)가 되어
  후반 라운드에서 스킬이 순식간에 다 자랍니다. 한 번 그렇게 만들었다가 되돌렸습니다.
  접촉 피해만 `TIER_DMG_POW`(0.3제곱)로 눌러 둡니다. 체력만큼 올리면 T3 가 한 방입니다.
- **상점의 `dmg`·`cd` 를 만지면 난이도가 통째로 따라 움직입니다.** 값을 바꿨으면
  `power(99)` 를 다시 구해 `D.TIER_MUL` 의 마지막 칸과 맞추세요. 티어를 하나 더 늘릴
  때도 등비의 밑을 `power(99)^(1/티어수-1)` 로 다시 구해야 합니다.
- **이 구조로 오기까지 두 번 실패했습니다.** (1) 라운드마다 체력에 배수를 곱했더니
  100라운드에서 65배가 되어, 똑같이 생긴 좀비가 라운드에 따라 14 이거나 878 이었습니다.
  (2) 요구 화력이 체력·스폰·웨이브 세 갈래로 갈라져, 100라운드는 초당 54마리가 쏟아져
  11초 만에 `ENEMY_CAP` 에 붙었고 강화를 다 해도 3분을 못 버텼습니다. 통로를 하나로
  줄인 것이 해결책이었습니다.
- **판 안의 곡선 두 개(`wave_at` 의 제곱 · `HP_RAMP_POW`)가 "라운드 앞부분"의 손잡이입니다.**
  둘 다 라운드 번호를 안 보므로 등식은 그대로고, 끝값도 안 건드립니다 — **꼭대기는 두고
  바닥만 올립니다.** 예전 값(웨이브 1.6제곱 · 체력 2제곱)에서는 0번 줄(초당 1.9마리, 기본
  종 하나)이 진행도 25%까지 갔고 체력 배율도 1.6배뿐이라, 5분짜리 라운드의 **첫 2분이
  좀비 몇 마리**였습니다("라운드 시작 1~3분이 너무 쉽다"). 지금은 1.15제곱 · 1.75제곱입니다.
  전체를 조이고 싶으면 여기가 아니라 `HP_RAMP` 를 쓰세요.
- **적이 쏜 것은 `P.VENOM`(분홍) 하나로 통일합니다.** 아군 탄(금·주황·옥색)에도 젬(초록)
  에도 안 쓰는 색이라야 "분홍은 피한다"가 굳습니다. 붉은색이던 시절 침은 붉은 티어 적·
  피격 알갱이·데미지 숫자에 묻혀 **날아오는 게 안 보였습니다.** 색만으로는 부족해서
  검은 테두리 + 맥동하는 경고 고리 + 자국으로 **형태**까지 다르게 하고(`Art.bullet` 의
  `spit`) 발사음(`Snd.spit`)을 붙였습니다. 새 원거리 적도 이 규칙을 따르세요.
- **후반으로 갈수록 커지는 값에는 상한을 두세요.** `round_bosses` 는 8 입니다. 상한이
  없으면 100라운드에서 49마리가 되어 난이도가 아니라 `ENEMY_CAP`·프레임 문제로 바뀝니다.
- **등식은 비율만 맞춥니다 — 기준점이 헐거우면 전부 헐거워집니다.** 라운드 100@만강은
  정의상 라운드 1@0강과 똑같은 난이도입니다. 그래서 "100라운드가 너무 쉽다"는 말은
  거의 항상 "1라운드가 너무 쉽다"는 뜻입니다. 실제로 등식이 완벽했는데도 진화 5종
  로드아웃이 필요 화력의 **11.8배**였습니다.
  전체를 조이거나 푸는 손잡이는 **`D.HP_RAMP` 하나**입니다(판 끝의 체력 배율). 모든
  라운드에 똑같이 걸려서 등식을 건드리지 않습니다. 라운드별로 손대고 싶어질 때마다
  그건 두 번째 통로를 만드는 것임을 떠올리세요.
- **진화는 5레벨의 2.0배로 맞추세요. 이건 지켜지지 않으면 바로 티가 납니다.** 한때
  2.1~6.05배까지 벌어져 있었고(번개 4.5배·냉기 6.1배·유도 5.3배), 그래서 스킬을
  다 진화시킨 판 후반이 오히려 제일 쉬웠습니다 — 판이 달아오르는 속도(스폰 11배 × 체력
  7배)보다 스킬이 자라는 속도가 훨씬 빨랐기 때문입니다. 진화 칸을 만졌으면
  `dmg × count / cd` 를 5레벨 칸과 나눠 보세요. 8종이 전부 2.0 근처여야 합니다.
- **월드는 `main.gd` 의 `ZOOM` 배율로 그립니다.** 카메라 변환을 한 번 걸지만
  `Spr.blit`·`Spr.limb_tex` 가 좌우 뒤집기 때문에 `draw_set_transform` 으로 그 변환을
  **덮어쓰므로** `Spr.zoom` 에도 같은 값을 넣어 줘야 합니다. 화면에 담기는 월드 크기
  (`_world_view`)도 배율만큼 넓어지니 카메라 고정 범위와 그리기 판정 범위를 그 값으로
  잡아야 맵 밖이 비치거나 화면 끝 적이 사라지지 않습니다. HUD 를 그리기 전에는
  `Spr.origin`·`Spr.zoom` 을 되돌려야 직업 선택창이 밀리거나 작아지지 않습니다.
- **그리기 등급(`Art.tier`)은 대수가 아니라 실제 프레임 속도로 정합니다**(`main.gd` 의 `_pick_tier`).
  0 = 전부 · 1 = 몸통과 머리 그림만 · 2 = 덩어리 두 개. **대수로 자르지 마세요** — 예전에는
  "화면 안 70마리 초과면 덩어리 두 개"였는데, 그 70 은 팔다리를 **벡터로** 그리던 시절의 값이고
  지금은 텍스처라 **300마리를 전부 그려도 60fps 가 나옵니다**(실측). 그래서 8라운드처럼 적이
  몰리면 멀쩡한데도 좀비가 전부 동그라미로 보였습니다. 판단 기준도 절대 fps 가 아니라
  **이 판에서 본 가장 빠른 프레임의 비율**입니다 — 주사율이 낮은 기기에서 늘 최하 등급이
  되지 않게 하려는 것입니다. 1단계를 건너뛰고 바로 원으로 내려가지 마세요.
- **스폰 배율을 두 번 세지 마세요.** 스폰 수는 `WAVE` 표 한 곳에만 있고 모든 라운드가
  같은 표로 달아오릅니다. 예전에 라운드 배율(`round_rate`·`round_burst`)을 그 위에 또
  곱했더니 100라운드에서 초당 54마리가 쏟아져 11초 만에 `ENEMY_CAP` 에 붙었습니다 —
  그 상태에서는 난이도를 조절하는 게 아니라 상한만 만지고 있는 셈이 됩니다.
- **`ENEMY_CAP` 을 올릴 때는 스킬 5개를 전부 진화시킨 상태로 재세요.** 적 그림만 보면 여유가
  있어 보이는데 투사체·타격 이펙트·효과음이 같이 불어납니다. 실측 표는
  [zombie-defense/README.md](zombie-defense/README.md) "성능" 항목에 있습니다.
- **`draw_multimesh` 로 적을 한 번에 그리려 하지 마세요.** 드로우콜이 프레임당 3,400개라
  많아 보이지만 병목은 GPU 가 아니라 **GDScript→엔진 호출 횟수**입니다(측정: CPU 시간이
  프레임 시간 전부). 멀티메시는 인스턴스마다 변환·색을 GDScript 로 채워야 해서 호출 수가
  오히려 늘어납니다. 실제로 듣는 지렛대는 **마리당 그리기 호출 수를 줄이는 것** 하나입니다 —
  적 600마리 기준으로 0단계(마리당 7~8번)는 7.4ms, 1단계(2번)는 1.0ms 입니다.
  그래서 `main.gd` 의 `TIER0_MAX_SHOWN` 으로 화면 안 170마리를 넘으면 미리 1단계로 내립니다.
- **프레임을 잡아먹는 곳이 그리기라고 단정하지 마세요.** 적 800마리에서 그리기를 최하 등급까지
  내려도 35fps 를 못 넘었는데, 범인은 **효과음 합성**이었습니다. `snd.gd` 는 샘플 단위
  GDScript 루프라 **살아 있는 목소리 수가 그대로 프레임을 먹습니다**(22개가 차 있으면
  프레임당 4~8ms). 적이 초당 수백 마리 죽으면 `Snd.kill()` 도 그만큼 불립니다 —
  그래서 자주 나는 효과음에 최소 간격(`Snd.MIN_GAP`)을 뒀습니다. **새 효과음을 넣을 때
  초당 여러 번 날 수 있으면 여기에 한 줄 추가하세요.**
- **이펙트 배열에서 `pop_front()` 로 오래된 것을 버리지 마세요.** 배열을 통째로 앞으로 당기는
  연산입니다. 상한이 꽉 찬 상태로 초당 수백 번 불리므로 `Fx._push_part` 처럼 자리를 돌려 씁니다.
  데미지 숫자는 `draw_string` 이라 특히 비쌉니다 — 100개가 뜨면 그리기에만 4ms 를 씁니다.

### 테스트

자동화된 테스트가 없습니다. 빌드 성공 + 실행으로 확인하고,
수치를 건드렸다면 최소 2~3분 직접 플레이해 레벨업 속도와 화면 밀도를 봅니다.

> **`get_viewport().get_texture().get_image()` 로 찍은 화면은 실제보다 훨씬 어둡습니다.**
> `hdr_2d` 라 이미지가 **선형 공간**으로 나오기 때문입니다 — 바닥색 `Color8(26,32,52)` 이
> PNG 에서는 `(3,4,10)` 으로 찍힙니다. 어두운 것끼리(바닥 26,32,52 · 격자 38,48,74)는
> 캡처에서 거의 구분이 안 됩니다. **"바닥이 안 그려졌다"고 오해하기 딱 좋으니**
> 캡처로 판단하기 전에 `sRGB → 선형` 변환을 되짚어 보세요(0.1 근처 값이 1/10 로 내려갑니다).

## dragon-dash (드래곤 대시)

Godot 4 / GDScript. **이 저장소에서 유일한 안드로이드 게임**입니다 — 세로 화면 종스크롤
무한 러너. 끌어서 좌우로만 움직이고 브레스는 자동, 부딪히면 하트가 깎이고 하트가 없으면
즉사, **점수 = 비행 거리 + 사냥 점수**입니다. 드래곤플라이트 계열입니다.

```
cd dragon-dash
build.bat         APK + Windows exe 전부
build.bat apk     APK 만
run.bat           소스에서 바로 실행 (항상 테스트 빌드)
soak.bat          헤드리스 무인 주행 — 수치를 만졌으면 여기부터
```

- **여기만 안드로이드입니다.** 배포물은 APK 이고, 나머지(노드 안 쓰기 · hdr 발광)는 다른
  Godot 게임과 같습니다. Windows exe 는 폰 없이 손볼 때 쓰는 **확인용**이지 배포물이 아닙니다.
- **Godot 은 SDK · JDK · 디버그 키스토어 경로를 환경 변수가 아니라 에디터 설정에서
  읽습니다** — `%APPDATA%\Godot\editor_settings-4.7.tres` 의
  `export/android/android_sdk_path` · `java_sdk_path` · `debug_keystore` 세 줄입니다.
  `ANDROID_HOME` 을 아무리 맞춰도 안 봅니다.
- **`window/handheld/orientation` 은 문자열이 아니라 정수 enum 입니다**(0=가로 1=세로).
  `"portrait"` 라고 적으면 조용히 무시되고 가로로 나갑니다 — 첫 APK 가 실제로
  `android.hardware.screen.landscape` 로 찍혀 나왔습니다. 내보낸 뒤에는
  `aapt2 dump badging` 으로 방향을 확인하세요.
- **안드로이드 내보내기는 `import_etc2_astc` 를 요구합니다.** 그림 파일이 하나도 없어서
  압축할 것이 없는데도 켜 두지 않으면 내보내기 자체가 거부됩니다.
- **터치는 `emulate_mouse_from_touch` 로 마우스 이벤트로 받습니다.** 조작 코드가 하나여야
  PC 와 폰이 같이 동작합니다.
- **드래그는 상대 이동이고 1:1 에 가깝습니다.** 절대 이동은 드래곤이 손가락 밑에 깔리고,
  속도 상한이 낮으면 급하게 그은 손가락을 몸이 못 따라와 "피하려던 순간에 안 피해집니다".

### 이 게임은 "쏘는 게임"입니다 — 여기를 먼저 읽으세요

**드래곤플라이트는 앞을 갈아버리며 나아가는 게임이지 틈을 찾아 빠져나가는 게임이
아닙니다.** 처음 만든 것이 후자였고, 원인이 넷이었습니다. 전부 되풀이하기 쉬운 것들이라
적어 둡니다.

1. **못 부수는 장애물을 만들지 마세요.** 통로 한 칸만 비우는 바위 벽을 넣었더니
   그 순간 "쏴서 길을 연다"가 "안 맞는 칸을 고른다"가 되어 장르가 통째로 바뀌었습니다.
   운석은 벽이 아니라 **날아오는 것**이고, 단단할 뿐 부술 수 있습니다.
2. **`clear`(뿌린 체력 대비 넣은 피해)가 80% 이상이어야 합니다.** 20~25% 였던 적이
   있는데, 그건 눈앞의 적 넷 중 셋이 그냥 살아서 지나가는 게임입니다.
   **원인은 대개 피해량이 아니라 브레스가 덮는 폭입니다** — `BASE_DMG` 만 올렸다가
   아무것도 안 나아졌습니다. 브레스는 알갱이가 아니라 **줄기**입니다.
3. **근접 처치 보너스가 이 게임의 심장입니다**(`D.CLOSE_BAND`). 없으면 화면 아래에
   붙어 멀리서 쏘는 것이 언제나 정답이라 드래곤을 움직일 이유가 사라집니다.
   **연속 곡선이 아니라 띠이고, 반지름이 아니라 세로 거리로 나뉩니다** — 드래곤 위로
   가로선을 긋고 `2X · 3X · 5X · 20X` 를 적습니다. 배율이 눈에 보이는 칸이라야
   "저기까지 끌어들여서 잡는다"는 판단이 생깁니다. **선을 그리는 곳과 점수를 계산하는
   곳이 같은 상수를 봐야 합니다.**
4. **점수는 거리 + 사냥입니다.** 거리만 세면 "안 쏘고 피하기만 하는" 것이 최적해가
   됩니다 — 실제로 무인 주행이 처치 3마리로 5470m 를 갔습니다.

### 난이도 — 통로가 셋입니다 (일부러)

**적 체력 · 스크롤 속도 · 구간 안의 밀도** 셋이 같이 오릅니다.
좀비디펜스·갈루가는 통로를 하나로 묶어 등식으로 검산하지만, **여기서는 일부러
그러지 않았습니다** — 러너는 여러 축이 한꺼번에 조여드는 것이 곧 재미이고, 원작
소개도 "더 멀리 갈수록 비행 속도가 빨라지고 적들이 강해진다" 한 줄입니다.
하나로 묶으면 10분을 달려도 첫 1분과 같은 게임이 됩니다.

**대신 대가를 치릅니다: 수치를 증명할 수 없습니다.** `soak.bat` 은 "크게 망가지지
않았나"만 보고, 난이도는 직접 2~3분 플레이해서 잡습니다.

- 적 체력 `D.hp_at(m)` · 스크롤 `D.scroll_at(m)` · 밀도는 웨이브 표가 **구간 안에서만**
  달아오르고 구간마다 똑같이 되풀이됩니다(표의 `at` 은 거리를 안 봅니다).
- **봇은 근접 배율을 노리지 않습니다** — 사람은 점수를 위해 일부러 파고들지만 봇은
  안전한 쪽만 고릅니다. **봇 점수를 사람 점수와 비교하지 마세요.** 회귀 확인용입니다.
- 현재 기준선(0강 · 화룡 · 봇): 거리 2769~4918m · `clear` 74~98% · 처치 137~250.

### 성장은 두 갈래입니다 — 섞지 마세요

- **판 안** = 파워업 `P`(거리마다 확정으로 하나씩) + **시간제 아이템 4종**
  (듀얼샷 · 자석 · 하이퍼 플라이트 · 더블 스코어). 죽으면 사라집니다.
  **넷 다 시간제여야 합니다** — 영구히 붙는 것을 여기 두면 상점과 구분이 사라집니다.
- **판 밖** = 상점(`D.SHOP`). 금화로 사고 영원히 남습니다. `user://save.cfg` 하나에
  저장하고, **읽기/쓰기가 실패해도 기본값으로 돌아야 합니다.**
- **하트 상한은 3입니다.** 즉사 게임에 "한 번 봐줌"은 필요하지만, 더 주면 부딪히는
  것이 실수가 아니라 자원 소모가 되어 긴장이 통째로 빠집니다.

### 보스

**구간 끝마다 나옵니다.** 러너가 끝없이 흐르기만 하면 리듬이 없습니다.

- **보스와 싸우는 동안 거리가 멈춥니다.** 안 멈추면 보스가 다음 구간까지 따라 들어와
  웨이브 표와 겹칩니다.
- **못 잡으면 날아갑니다**(`D.BOSS_TIMEOUT`). 반드시 잡아야 넘어가게 하면 화력이
  모자란 순간 그 자리에서 게임이 멈춥니다 — 러너에서 제일 나쁜 상태입니다.
  대신 빨리 잡을수록 점수가 큽니다.
- **예비 동작 없이 나가는 공격을 만들지 마세요.** 방향은 예비 동작이 시작될 때 박아
  두고 안 고칩니다. **세 패턴의 경고 모양이 서로 달라야** "뭔가 온다"에서
  "어디로 피한다"가 됩니다.

### 지켜야 할 제약 (dragon-dash)

- **노드를 만들지 마세요.** 적·탄·금화는 `Dictionary` 배열이고 `_draw()` 한 곳에서 그립니다.
- **적을 죽일 때 배열에서 바로 빼지 마세요** — `dead` 표시만 남기고 `_sweep()` 이 정리합니다.
- **둥근 것은 `draw_circle` 로.** `G2.fill_fan` 은 호출마다 그리기 명령이 생겨 배칭이 끊깁니다.
- **이펙트에 `pop_front()` 를 쓰지 마세요** — `sparks` 처럼 자리를 돌려 씁니다.
- **관통 브레스에는 `D.REHIT` 간격이 필요합니다.**
- **지형과 같이 흘러가는 것은 `scroll_at(m)` 배율을 같이 받습니다.**
- **적이 쏜 것은 분홍(`P.VENOM`) 하나로 통일합니다.** **운석만 발광이 없고**(빛나는 것은
  대체로 주울 것이라 어두운 것이 눈에 갈립니다), **불꽃 몬스터는 반대로 혼자 이글거립니다**
  (죽으면 터지므로 "이걸 먼저 노린다"가 읽혀야 합니다).
- **새 화면을 만들면 키 분기와 탭 분기를 둘 다 채우고**, 자리 함수를 하나 두어
  그리기와 탭 판정이 같이 보게 하세요(`Sel.card_rect`·`Shop.row_rect`·`Hud.over_rect`).
- **화면 흔들림은 월드에만 걸고 HUD 전에 `draw_set_transform` 을 되돌리세요.**
- **HUD 가운데 위는 비워 둡니다** — 적이 거기서 내려옵니다. 재화는 왼쪽 위, 거리는 오른쪽 위.
- **드래곤 셋은 세기가 같습니다**(`D.POWER_DPS`). 다른 것은 브레스가 자라는 방향뿐입니다.

### 테스트

자동화된 테스트가 없습니다. **빌드 성공 + 실행 + `soak.bat`** 이고,
수치를 건드렸다면 `clear` 와 거리·점수가 크게 움직였는지 본 뒤
**최소 2~3분 직접 플레이**합니다.


## beatfall (비트폴)

Godot 4 / GDScript. 세로 낙하형 4·6키 리듬게임입니다. **mp4 를 넣으면 오디오를
분석해 채보를 자동으로 만듭니다** — 곡은 exe 옆 `songs/` 폴더에서 실행 중에
읽으므로 곡을 더 넣는다고 다시 빌드하지 않습니다.

```
cd beatfall
run.bat            소스에서 바로 실행
build.bat          내보내기 (dist\beatfall.exe + dist\songs\)
check.bat          자체 검사 — 수치를 만졌으면 여기부터
check.bat sweep    온셋 문턱 격자 탐색
check.bat demo-song  데모 곡을 wav 로 굳혀 자동 채보까지 태운다 (ffmpeg 불필요)
import.bat <파일>  mp4/mp3/wav 에서 곡 하나 만들기 (ffmpeg 필요)
```

엔진·발광 처리는 다른 Godot 게임과 같습니다(`GODOT_BIN`, `hdr_2d`, 색 1.0 초과 시 번짐).

### 오디오 시계가 전부입니다 — 여기를 건드리기 전에 반드시 읽으세요

[src/conductor.gd](beatfall/src/conductor.gd) 가 이 게임에서 제일 중요한 파일입니다.

- **`_process` 의 delta 를 더해서 곡 위치를 세지 마세요.** 프레임이 한 번 튀면
  그 오차가 영구히 남고, 3분짜리 곡이면 끝에서 100ms 넘게 밀립니다. 그 상태에서는
  판정창을 아무리 다듬어도 곡 후반이 통째로 안 맞습니다.
- 진짜 시계는 `AudioStreamPlayer.get_playback_position()` 이고, 여기에
  `AudioServer.get_time_since_last_mix()` 를 **더하고**(그 값은 믹스 블록 단위로만
  갱신되어 프레임마다 계단처럼 움직입니다) `AudioServer.get_output_latency()` 를
  **뺍니다**(안 빼면 화면이 소리보다 빨라집니다).
- **판정용(`pos`)과 그리기용(`smooth`) 시계를 나눠 두었습니다.** 원값을 그대로
  그리면 노트가 떨리고, 다듬은 값으로 판정하면 정확도를 잃습니다.
- **캘리브레이션(곡 선택 화면 `C`)은 장식이 아닙니다.** 위 둘로도 기기 차이가
  남습니다 — 블루투스 이어폰은 100ms 넘게 밀립니다. 여러 번 두드린 값의
  **중앙값**을 쓰세요. 평균은 실수로 크게 어긋난 입력 하나에 통째로 끌려갑니다.
- **자동 연주는 전부 PERFECT 가 나와야 합니다.** 치는 쪽도 판정하는 쪽도 같은
  `Conductor.pos` 를 보기 때문입니다. GREAT 이하가 섞이면 시계가 두 갈래로
  흐르고 있다는 뜻이고, 그게 이 게임에서 제일 찾기 어려운 종류의 버그입니다.
  `run.bat -- --autoplay --song=0 --diff=2` 로 확인합니다.
- **자동 연주는 게임 안에서도 `F2` 로 켭니다**(곡 선택 화면). `--autoplay` 는 곡
  선택을 건너뛰고 바로 시작하는 것뿐입니다. 켜면 기록이 남지 않습니다.
- **아래쪽 토글은 자리 함수 하나(`Sel.toggle_rect`)를 그리기와 클릭이 같이 봅니다.**
  키만 넣으면 "마우스로 못 켠다"를 눈치채기 어렵습니다 — 자동 연주(F2)가 실제로
  그렇게 아무도 모르는 기능이 되어 있었습니다.
- 자리 배치를 만졌으면 `run.bat -- --shot=out.png` 으로 캡처해서 보세요.
  **캡처는 `hdr_2d` 때문에 실제보다 훨씬 어둡습니다** — 밝기 말고 자리만 봅니다.

### 키음 — 노트를 친 순간에 그 자리의 음이 납니다

[src/keys.gd](beatfall/src/keys.gd). 비트매니아·BMS 의 구조를 **믹스된 곡에서**
흉내 낸 것입니다. 원리는 하나 — 곡을 노트 시각으로 잘라 조각을 만들고 각 조각을
그 노트에 붙입니다. PERFECT 면 제자리에서, GOOD 이면 120ms 밀려서 나오고,
MISS 면 아예 안 납니다.

- **조각은 곡을 빈틈없이 덮어야 합니다.** 이어 붙이면 원곡이 그대로 나와야 하고,
  틈이 있으면 다 맞혀도 그 자리가 비고 겹치면 같은 소리가 두 번 나서 울립니다.
  귀로는 "원래 이런 곡인가" 싶어 못 잡으니 `check.bat` 의 `[1] 키음 조각표 검사`
  가 틈·겹침·짝 없는 노트를 셉니다.
- **같은 시각의 노트(동시치기)는 조각 하나를 같이 씁니다.** 노트마다 조각을 주면
  길이 0 짜리 조각이 생겨 아무 소리도 안 납니다.
- **곡 전체를 노트에 매달면 안 됩니다.** 그래서 두 층입니다 —
  `bed`(180Hz 아래, 계속 흐름) + `keys`(나머지, 조각으로 잘림).
  **바닥층이 [Conductor] 의 시계이기도 합니다.** 곡 전체가 노트에 매달리면
  음이 빌 때 판정의 기준 시계가 같이 사라집니다. 그건 게임이 안 되는 상태입니다.
- **층을 나눌 때 1차(6dB/oct) 필터를 쓰세요.** 1차 저역과 1차 고역은 **더하면
  정확히 원신호로 돌아옵니다.** 더 가파른 필터는 층끼리 덜 새는 대신 합이 원곡이
  아니게 되고, 그러면 "다 맞히면 원곡 그대로"가 거짓말이 됩니다.
  `import.bat` 이 매번 잔차를 재서 찍습니다(지금 level5 기준 −39.4dB,
  데모는 진짜 스템 분리라 최대 오차 1/32000).
- **조각을 미리 잘라 [AudioStreamWAV] 로 만들지 마세요.** 4분짜리는 노트가
  2천 개라 곡이 메모리에 여러 벌 올라갑니다. 스트림 하나를 목소리 넷이 나눠 쓰고
  `play(시작 위치)` 로 그 자리부터 틀 뿐입니다 — 조각은 (시작, 끝) 한 쌍입니다.
- **조각은 자기 자리부터 틀고 제 끝에서 멈춥니다.** 늦게 쳤으면 늦게 시작해 짧게
  끝나서 "밀렸다가 따라붙는" 소리가 됩니다. 시작 위치까지 같이 밀면 그냥 곡이
  느려진 것처럼 들려서 "틀렸다"가 안 읽힙니다.
- **`Keys` 는 오토로드를 참조하면 안 됩니다.** 볼륨을 `setup()` 인자로 받는 이유가
  이것입니다 — `--script` 모드에는 오토로드가 없어서 `Sv` 를 쓰면 **컴파일 자체가
  안 되고**, 그러면 헤드리스 검사에서 조각표를 만들 수 없습니다.
  같은 이유로 `check.bat` 은 [Play] 를 안 씁니다(play.gd 가 `Snd` 를 참조합니다).
- **두 층이 다 있어야 켜집니다.** 하나만 있으면 소리가 반쪽이 나므로 아예 안 쓰고
  `audio.ogg` 한 벌로 예전처럼 재생합니다 — 옛 곡 폴더가 그대로 돌아야 합니다.
  곡 선택 화면 `K` 로 끌 수도 있습니다.
- **키음이 켜지면 음이탈을 약하게 겁니다**(`D.WARP_WITH_KEYS` = 0.45).
  둘 다 세게 걸면 서로 잡아먹습니다 — 틀린 자리에서 소리가 아예 비는 것만으로
  충분히 읽히고, 음이탈은 거들기만 하면 됩니다.
- **진짜 BMS 와 다른 점을 먼저 말하세요.** BMS 는 작곡가가 악기별 트랙을 따로
  만들어 둬서 한 악기만 빠지지만, 믹스를 자르면 **그 구간의 모든 악기가 같이**
  빠집니다. 이미 섞인 소리는 다시 나눌 수 없습니다. 바닥층은 그 거칠음을
  덜어 주는 장치이지 해결책이 아닙니다.

### 음이탈 — 키음을 거드는 장치

[src/warp.gd](beatfall/src/warp.gd). **이 게임에는 노트를 칠 때 나는 타격음이
없습니다**(`Sv.vol_hit` 기본값 0). 리듬게임의 타격음은 원곡 위에 없는 소리를
얹는 것이라 곡을 방해하는데, 이 게임의 목적은 원곡을 그대로 들려주는 것입니다.
그래서 **잘 치는 것에 대한 보상이 "원곡이 깨끗하게 들리는 것" 자체**이고,
틀리면 그 원곡이 무너집니다(`Play.warp` → `Warp.apply`).

- **타격음 기본값을 되돌리지 마세요.** 계속 소리가 나면 음이탈과의 대비가 통째로
  사라집니다. 켜고 싶은 사람을 위해 곡 선택 화면 `H` 로 남겨 두었을 뿐입니다.
- **메뉴 소리와 캘리브레이션 메트로놈은 타격음과 따로 매달아야 합니다**
  (`Sv.vol_ui`). 같이 매달면 타격음이 꺼져 있을 때 메트로놈이 안 나서
  판정 보정 화면이 아무것도 못 하는 화면이 됩니다.
- **효과는 음악 버스에 항상 얹혀 있습니다.** 그래서 중립값이 하나라도 어긋나면
  잘 치는 사람의 곡이 조용히 상합니다 — 특히 `AudioEffectLowPassFilter` 의
  기본 차단 주파수가 **2000Hz** 라, 얹기만 하고 안 올려 두면 멀쩡한 곡이 처음부터
  먹먹하게 들립니다. 귀로는 "원래 이런 곡인가" 싶어서 눈치채기 어렵습니다.
  `check.bat` 의 `[0] 음이탈 중립 검사`가 이걸 못 박습니다.
- **`AudioEffectPitchShift` 를 쓰지 마세요.** 음정을 내리는 게 제일 곧이곧대로지만
  ① FFT 기반이라 얹는 것만으로 지연이 생기고, 그 지연은
  `AudioServer.get_output_latency()` 에 안 잡혀 [Conductor] 가 모릅니다.
  ② `pitch_scale = 1.0` 이어도 그래뉼러 처리가 원음을 상하게 해서
  **"다 맞히면 원곡 그대로"라는 전제가 깨집니다.** 지금 쓰는 셋(로우패스 ·
  코러스 · 볼륨)은 중립값에서 수학적으로 통과입니다.
- **효과를 판 도중에 켜고 끄지 마세요.** 값만 바꿉니다. 효과를 붙였다 뗐다 하면
  버스의 처리 지연이 바뀌어 소리와 판정이 어긋납니다.
- **음이탈은 화면에도 표시합니다**(`Art._warp`). 소리가 무너지는 것이 본 신호지만
  처음 치는 사람은 소리가 왜 이상해졌는지 모릅니다 — 화면이 같이 흐려져야
  "내가 틀려서 그렇구나"가 읽힙니다. 위에서 내려오는 붉은 기운이라 판정선의
  타격 섬광과 헷갈리지 않습니다.
- **진짜 키음(BMS 방식)은 못 합니다.** 비트매니아는 곡을 노트 하나당 음원 하나로
  쪼개 두고 친 노트의 음원만 재생하는데, 그건 작곡가가 트랙을 따로 만들어 뒀기
  때문입니다. **이미 믹스가 끝난 mp4 에서는 소리를 다시 분리할 수 없습니다.**
  요청이 들어오면 이 한계를 먼저 말하고 지금 방식을 설명하세요.
- 세기를 맞출 때는 `run.bat -- --autoplay --song=0 --diff=2 --miss=8` 로 듣습니다 —
  자동 연주가 8노트마다 하나씩 일부러 놓칩니다. 사람이 일부러 틀려 가며 듣는
  것보다 고르게 들립니다.

### 지켜야 할 제약

- **수치는 전부 [src/data.gd](beatfall/src/data.gd) 에 모읍니다.** 판정창·점수·체력·
  배속·레인 배치·자동 채보 사양이 한 파일에 있습니다.
- **판정창(`D.WINDOW`)은 사람 손의 한계가 아니라 화면과 소리의 지연을 감안한
  값입니다.** 60Hz 화면은 프레임 하나가 16.7ms 이고 오디오 출력 지연이 보통
  15~30ms 입니다. PERFECT 를 20ms 로 조이면 아무리 잘 쳐도 안 뜨는 기기가 생깁니다.
- **노트 시각은 박이 아니라 초입니다.** 박으로 두면 BPM 이 흔들리는 실제 녹음에서
  곡 후반이 통째로 밀립니다 — mp4 에서 뽑은 곡은 클릭 트랙에 맞춰 연주한 것이
  아니므로 대부분 흔들립니다. BPM 은 격자 표시와 채보 생성기가 참고할 뿐입니다.
- **체감 난이도는 노트 밀도가 아니라 `D.SCROLL_SEC` 가 정합니다.** 값이 크면
  촘촘한 구간이 뭉쳐 보이고, 작으면 노트가 갑자기 튀어나옵니다.
- **한 손으로 못 치는 배치를 만들지 마세요.** 같은 레인에 `D.AN_SAME_LANE_GAP`
  (85ms) 안에 두 번 오면 안 됩니다. `check.bat` 이 "겹침" 으로 셉니다.
- **롱노트는 누르고 있는 동안 그 레인을 통째로 차지합니다.** 자리 배정에서
  끝나는 시각(`t + dur`)을 넣어 둬야 그 위에 다른 노트가 얹히지 않습니다.
  시작 시각만 넣으면 손이 하나 모자란 자리가 조용히 생깁니다.
- **판정은 레인마다 따로 커서를 밉니다.** 한 배열을 앞에서부터 훑으면 레인 0 의
  노트를 기다리는 동안 레인 3 의 노트가 커서에 막힙니다.
- **노드를 만들지 마세요.** 노트는 `Dictionary` 배열이고 `_draw()` 한 곳에서
  그립니다. 3분 채보는 노트가 2천 개입니다.
- **노트 한 장은 그리기 호출 두 번**(몸통 + 윗면)입니다. 빠른 구간에는 화면에
  60~80장이 깔리므로 한 겹만 늘어도 비용이 그만큼 곱해집니다.
- **효과음은 켤 때 한 번 렌더해 [AudioStreamWAV] 로 굳혀 둡니다**
  ([src/snd.gd](beatfall/src/snd.gd)). 좀비디펜스처럼 매 프레임 샘플 단위로
  합성하면 초당 10번 넘게 나는 타격음이 그대로 프레임을 먹고, **프레임이 떨어지면
  노트가 떨려서 못 칩니다** — 소리 때문에 게임이 안 되는 셈입니다.
- **화면 가운데 위아래는 비워 둡니다.** 노트가 거기서 내려오고 거기서 판정됩니다.
  점수·체력은 필드 양옆입니다. **콤보만 판정선 위**에 둡니다 — 시선이 이미 거기
  고정돼 있어서, 옆으로 빼면 콤보가 끊긴 걸 눈치채지 못합니다.
- **레인 색과 판정 색이 겹치면 안 됩니다.** 노트는 청록·보라, 판정은 금·초록·
  주황·적입니다. 겹치면 화면이 빨라졌을 때 잘 치고 있는 건지 노트가 쌓인 건지
  순간적으로 구분이 안 됩니다. **레인마다 다른 색을 주지 마세요** — 바깥(양 끝)과
  안쪽 두 가지뿐입니다. 예뻐 보이지만 빠른 구간에서 색이 정보가 아니라 소음이 됩니다.
- **새 화면을 만들면 키 분기와 마우스 분기를 둘 다 채우고**, 자리 함수를 하나 두어
  그리기와 클릭 판정이 같이 보게 하세요(`Sel.song_rect` · `Sel.diff_rect`).
- **`_draw()` 에는 클리핑이 없습니다.** 노트는 `clip_contents` 를 켠 `Control`
  자식 안에서 그리고, HUD 는 그 **다음에** 추가한 레이어에 그립니다.
- **GDScript 의 `Packed*Array` 는 값 타입(copy-on-write)입니다.** 함수 인자로 넘겨서
  안에서 고치면 **호출한 쪽에 반영되지 않고**, 게다가 호출마다 배열이 통째로
  복사됩니다. FFT 와 곡 합성이 실제로 그 모양이었고 — 결과가 조용히 틀리는 데다
  곡 하나 만드는 데 몇 분이 걸렸습니다. 큰 버퍼를 다루는 루프는 **함수로 빼지 말고
  펼쳐 두세요.** 같은 이유로 `Array` 안에 담은 packed 배열에 `arr[i].append(x)` 를
  쓰면 안 됩니다(메서드 호출은 되쓰기가 안 됩니다). 중첩 첨자 대입 `a[i][j] = v`
  는 됩니다.

### 자동 채보 (`src/analyze.gd`)

**게임 안에서 돌지 않습니다.** `import.bat` 이 헤드리스 Godot 으로 한 번 돌리는
도구라, 3분짜리 곡에 십수 초가 걸려도 괜찮고 대신 정확도를 우선합니다.
지금 성능은 데모 곡 기준 재현율 91.6% · 정밀도 93.6% · 시각 편차 +0.2ms 입니다.

- **문턱을 감으로 정하지 마세요.** `check.bat sweep` 이 격자를 훑어 재현율과
  정밀도를 같이 보여 줍니다. 하나를 올리면 다른 하나가 내려가는 맞바꿈이라
  한쪽만 보고 고치면 반대쪽이 조용히 나빠집니다.
- **`D.AN_THRESH_SD` 를 올리지 마세요.** "근처에 큰 소리가 있으면 문턱이 같이
  올라간다"는 뜻이라, 스네어 옆의 하이햇처럼 **조용하지만 박을 이루는 소리가
  통째로 사라집니다.** 1.10 이던 시절 재현율이 67% 였고, 놓친 것이 정확히
  엇박(킥·스네어가 없는 8분음표)이었습니다 — 채보에 4분음표만 남아 밋밋해집니다.
- **온셋 시각은 FFT 창의 시작이 아니라 가운데입니다.** 창이 46ms 라 소리는 그 안
  어디에서든 날 수 있고, 시작으로 잡으면 채보가 곡보다 평균 20ms 빨라집니다.
  PERFECT 창이 ±42ms 이므로 그대로 두면 정확히 친 사람이 GREAT 를 받습니다.
- **소리가 언제 끝나는지는 밀집된 편곡에서 정확히 잴 수 없습니다.** 대역 에너지가
  다음 소리 전까지 기준선으로 안 돌아오는 일이 흔합니다. 그래서 `An._sustain` 은
  보수적입니다 — **끝나는 것을 실제로 못 봤으면(상한에 걸려 멈췄으면) 롱노트로
  만들지 않습니다.** 없는 롱노트는 손가락을 붙잡아 그 뒤 노트까지 놓치게 해서
  없는 일반 노트보다 훨씬 나쁩니다. 이 판단을 완화했다가 채보의 60~90%가
  롱노트가 된 적이 있습니다. 지금은 6~11% 입니다.
- **밀도를 맞출 때 세기 순으로 위에서 n 개를 자르면 안 됩니다.** 후렴에 전부 몰리고
  조용한 구간이 통째로 비어서 노트가 아예 없는 30초가 생깁니다. 곡을 4초 구간으로
  나눠 구간마다 몫을 배정하고 그 안에서 세기 순으로 고릅니다(`An.pick`).
- **격자에 억지로 붙이지 마세요.** 가까우면 붙이고 멀면 원래 시각을 그대로 둡니다.
  실제 녹음은 BPM 이 흔들려서 전부 붙이면 리듬이 아니라 다른 곡이 됩니다.

### 테스트

자동화된 테스트는 `check.bat` 하나입니다. 두 가지를 봅니다.

1. **데모 채보가 사람이 칠 수 있는 모양인가** — 시각 순서, 한 손 겹침, 롱노트 비율.
2. **자동 채보가 실제 소리를 찾아내는가** — 내장 데모 곡([src/demo.gd](beatfall/src/demo.gd))
   은 **소리와 채보가 같은 표(`events()`)에서 나오므로 정답을 압니다.** 그 곡을
   분석기에 넣고 악기별 재현율·정밀도·시각 편차를 잽니다. 실제 mp4 로는 이 검산이
   불가능하므로, **데모에 새 악기를 넣는 것이 곧 분석기 시험을 늘리는 것**입니다
   (지속음 판정을 확인하려고 패드를 넣었습니다).

수치를 만졌으면 `check.bat` → 자동 연주 → **직접 2~3분 플레이** 순으로 봅니다.
배속과 판정 보정은 사람이 쳐 봐야만 알 수 있습니다.

## proyamae (프야매)

Godot 4 / GDScript. **KBO 실제 기록으로 만든 야구 매니저**입니다. 선수를 조작하지
않고 카드를 모아 오더를 짜며, 경기는 자동으로 굴러갑니다. 카드 한 장 =
**어느 선수의 어느 시즌**(`이승엽 03'`)이고 스텟은 그 시즌 실제 기록에서 나옵니다.

```
cd proyamae
fetch.bat          KBO 기록실 수집 (헤드리스 브라우저, 1회성 · 시즌당 약 90초)
check.bat          수집한 원본 검사 — 수집 뒤에 반드시 돌리세요
convert.bat        원본 기록 → 카드 6스텟
run.bat            소스에서 실행
build.bat          내보내기 (dist\proyamae.exe + dist\data\players\)
build-test.bat     테스트 빌드 (dist\proyamae-test.exe — 모든 카드 해금)
run.bat -- --simtest=500    경기 시뮬레이션 검산
run.bat -- --clicktest      화면마다 클릭 검사 (세이브를 비웁니다)
run.bat -- --blocktest      스킬블록 4×4 판 검산
run.bat -- --colortest      팀컬러 검산 (단일팀 · 단일연도 · 왕조 · 듀얼팀)
run.bat -- --unlock         모든 카드를 가진 상태로 실행
run.bat -- --deckscan       단일구단·단일연도·왕조로 짤 수 있는 최대 코스트
run.bat -- --deckfight      코스트 제한 없이 세 갈래 최강 덱 맞대결
```

### 수집기 — 조용히 틀린 자료를 만드는 것들

**KBO 기록실은 curl 로 못 긁습니다.** `__VIEWSTATE`·`__EVENTVALIDATION`·세션 쿠키를
다 맞춰도 **유효한 `__EVENTTARGET` 이나 실제 폼 필드가 하나라도 들어가면 서버가 500**
을 냅니다(없는 필드를 보내면 오히려 정상 응답이 옵니다). 그래서 헤드리스 크롬을
띄우고 CDP(WebSocket)로 진짜 브라우저를 조종합니다. 다른 출처도 다 막혀 있습니다 —
Baseball-Reference 403 · Statiz 도메인 소멸 · Kaggle 로그인 · 네이버 API 403.

여기서 겪은 것은 전부 **화면에는 멀쩡해 보이는데 자료만 틀리는** 종류입니다.

- **페이지의 `__doPostBack()` 을 부르지 마세요.** 함수는 있는데 실제 전송이 안 되는
  경우가 있어서, 드롭다운 값만 바뀌고 문서는 그대로인 채 대기만 하다 끝납니다 —
  화면상 "2003"인데 내용은 2026 이었습니다. `__EVENTTARGET` 을 직접 채우고
  `document.forms[0].submit()` 으로 보내세요.
- **`select.value` 로 결과를 확인하면 안 됩니다.** 그건 우리가 방금 써 넣은 값이라
  포스트백이 아예 안 걸렸어도 통과합니다. **`option.defaultSelected`** 가 서버가 보낸
  HTML 의 selected 속성이라 진짜 결과입니다. 이걸 안 봐서 실패가 오래 숨어 있었습니다.
- **구단을 바꿀 때 `hfPage` 를 1로 되돌리세요.** 앞 구단의 2페이지에 선 채로 구단을
  바꾸면 새 구단의 2페이지를 달라고 해서 빈 표가 옵니다 — 두산이 0명, 삼성이 6명으로
  들어와 있었습니다.
- **`tab_from` 이 있는 페이지는 주소로 바로 열면 안 됩니다.** KBO 사이트 버그입니다 —
  타자 `Basic2` 는 시즌을 바꿔도 **구단 목록만 현재 시즌 것**을 그대로 둡니다. 그래서
  2000~2007 년에만 있던 현대가 목록에 없어 통째로 빠집니다(없는 값을 넣으면
  EventValidation 이 막습니다). 앞 페이지에서 시즌을 고른 뒤 **사이트 안의 탭 링크를
  눌러** 넘어가면 시즌이 유지되고 구단 목록도 그 시즌 것으로 옵니다.
- **모든 조작은 결과를 확인하고 넘어가야 합니다.** 구단을 골랐으면 표 첫 행의 팀명이
  실제로 그 팀인지, 페이지를 넘겼으면 첫 행이 바뀌었는지 보고 아니면 다시 합니다.
- **`check.bat` 을 수집 뒤에 반드시 돌리세요.** 표 하나가 통째로 비어도 수집기는
  "완료"라고 말합니다. 그 상태로 변환하면 그 시즌 선수 전원이 장타력·정신력을
  평균값으로 받는데, 화면에는 멀쩡한 카드로 보여서 **눈으로는 절대 못 잡습니다.**
- 공개 사이트를 훑는 것이므로 요청 간격(`POLITE_MS`)을 줄이지 마세요.

### 변환 — 상대평가가 핵심입니다

- **스텟은 그 시즌 리그 안에서의 z-score 로 매깁니다.** 2014년 리그 타율은 .289,
  2006년은 .255 였습니다. 같은 자로 재면 타고투저 시즌 카드가 통째로 세지는데,
  한 덱에 여러 시즌이 섞이는 게임이라 바로 티가 납니다.
- **타석·이닝이 적은 선수는 평균 쪽으로 당깁니다.** 6타수 2안타를 그대로 환산하면
  교타력 90짜리 백업이 나옵니다.
- **종합(ov)도 시즌 안에서 다시 펴야 합니다.** 6스텟 평균을 그대로 쓰면 한두 개가
  특출한 선수도 나머지가 평균이라 종합이 안 오릅니다 — 2003 최고가 이승엽 OV70
  (56홈런)이라 EX·LEGENDS 가 한 장도 안 나왔습니다. 타자와 투수는 **따로** 폅니다.
- **COST 6 에 몫을 몰아 6→7 계단을 만듭니다**(`COST_SHARE` = [6,9,11,12,13,**30**,7,5,4,3]).
  구간이 넓으면 그 평균이 아래로 끌려가서 계단이 생깁니다 — `D.COST_HIGH`(7코부터
  테두리 한 겹 더)가 가리키는 경계입니다. 지금 6칸 평균은 48.6 51.0 52.5 53.6 54.7
  58.1 │ 64.0 67.2 70.2 75.3 이고 계단은 2.4 1.5 1.1 1.1 3.4 │**5.9**│ 3.2 3.0 5.1 입니다.
- **맨 위 COST 구간은 열려 있어 9→10 이 저절로 벌어집니다.** 꼭대기 압축
  (`ST_SOFT` 72 · `ST_SOFT_K` 0.30)으로 눌러 5.1 까지 내렸습니다 — 86 이던 시절
  COST 7→10 이 62.5 → 82.0(3단계에 19.5)으로 하위 구간(1→4 가 5.7)과 딴판이었습니다.
  **`COST_SHARE` 와 `ST_SOFT` 는 같이 움직이니 한쪽만 만지지 마세요.**
- **스텟에는 바닥 압축(`ST_FLOOR`)이 있습니다.** 위(`ST_SOFT`)의 짝입니다. 없으면
  그 시즌 하위권 구단의 카드가 통째로 밑바닥에 깔려서, 그 구단·시즌으로 왕조를
  맞춰도 최고 등급에서 아무것도 못 합니다 — 롯데 2003 왕조가 성장을 다 부어도
  **월드 시리즈 9.7등**이었습니다. 지금은 2.4등입니다.
- **종합(OV)은 확정된 스텟의 가중 평균입니다.** 여기서 **다시 z 를 내지 마세요** —
  z 는 척도를 정규화하므로 바닥 압축을 그대로 되돌립니다. 스텟이 이미 시즌 안에서
  상대평가된 값(`_to_stat`)이라 그 가중 평균이 곧 시즌끼리 비교 가능한 종합입니다.
  lift 를 얹기 **전** 스텟으로 OV 를 내던 예전 방식은 카드에 찍힌 여섯 칸과 OV 가
  따로 놀았습니다.
- **OV 분포를 바꾸면 그 위에 얹힌 문턱을 전부 다시 잡으세요.** OV 중앙값이
  44 → 55 로 옮겨지면서 `D.PACK_ODDS`(92/84/74/63) · `GRADES`(EX 92) ·
  `D.TIERS`(44/55/66/77/88) 가 한꺼번에 어긋났습니다 — EX 가 1만 장 중 28장
  (0.28%)이 되고 최고 리그가 너무 높아졌습니다. 지금은 각각 상위 1.5% · 6.5% ·
  18.5% · 43.5% 자리(77/72/64/56)이고, 등급은 51/58/65/72/79 입니다.
- **팀 평균을 리그 평균 쪽으로 당깁니다**(`TEAM_PULL` = 0.5). COST 는 스텟 합의
  **백분위(등수)** 라 스텟을 아무리 눌러도 등수가 그대로여서, 왕조 덱의 COST 폭이
  안 줄어듭니다(압축을 세게 해도 62 → 55 가 한계였습니다). **팀 사이의 차이 자체를
  줄여야** 합니다 — 지금은 137~176(폭 39)입니다. **이건 실제 기록에서 벗어나는
  조정입니다**: 그 시즌 꼴찌 팀 선수라는 이유로 스텟이 올라갑니다.
- **타자에게 `HIT_LIFT`(+3)를 얹습니다.** 타선이 세지면 선발이 실점으로 더 자주
  강판되고(`D.PULL_RUNS` = 4), 그만큼 중계 칸이 실제로 쓰입니다.
- **중계 체력을 올리지 말고 셋업·마무리를 내려 서열을 만드세요.** 중계를 올리면
  선발도 같이 올려야 서열이 유지되는데, 기력이 **체력의 제곱**이라 선발이 훨씬
  깊게 가서 **중계가 오히려 덜 나옵니다** — 중계 +20 · 선발 +12 로 잡았더니 중계
  등판이 0.53 → **0.18회/경기** 로 줄고 완투가 13.9% 로 뛰었습니다. 지금은
  `STAM_ROLE` = {선발 0 · 중계 −8 · 셋업 −19 · 마무리 −27} 로 66/55/51/48 입니다.
- **불펜 사용은 선발 카드를 고정해서 재세요.** `build_ai` 는 보직을 안 보고 `rot` 을
  뽑아서 상대 선발이 마무리 카드일 수 있고, 그러면 이닝 수가 통째로 흔들립니다.
  현재 실측(체력 66 선발): 5.69이닝 · 교체 2.53회 · 중계 등판 0.60회 · 완투 0.1%.
- **팀컬러 크기는 "제일 약한 왕조가 최강 단일구단·단일연도를 이기는가"로 정합니다.**
  왕조는 한 구단 한 시즌 안에서 25명을 채워야 해서 평균 OV 가 15 쯤 낮고(롯데 2003
  57.6 · 단일구단 최강 72.5) COST 도 상한을 한참 못 씁니다(133 대 190). 24/18 대
  15/11 이던 시절 롯데 2003 이 단일구단에 **36%** · 단일연도에 **20%** 였습니다.
  지금은 34/26 대 10/7 대 12/9 로 **72% · 54%** 입니다. **순서만 맞추고 크기를
  안 재면 조용히 뒤집힙니다.**
- **왕조 사거리 기준선** — 제일 약한 왕조(롯데 2003 · 평균 OV 58.4)가 월드 시리즈
  1.62등(3위내 92%), 제일 센 왕조(삼성 2015 · 66.0)가 1.00등(승률 .840)입니다.
  `ST_FLOOR` 나 `TIERS` 를 만졌으면 이 두 값을 다시 재세요.
- **투수 종합에서 체력 비중을 키우지 마세요.** 체력은 선발/불펜을 가르는 표시이지
  잘하고 못하고가 아닙니다. 비중을 크게 뒀더니 이닝이터가 그 해 최고 투수를 눌렀습니다.
  같은 이유로 **체력에는 `OVERALL_LIFT` 를 안 겁니다** — 걸면 좋은 마무리가 허드렛일
  중계보다 체력이 높게 나와서 **선발 > 중계 > 셋업 > 마무리 서열이 뒤집힙니다**
  (실제로 중계 42 · 마무리 46 이었습니다). 체력은 `ip_g`(등판당 이닝)가 0.80 이고,
  기록으로 안 갈리는 셋업·마무리만 `STAM_ROLE` 로 벌려 둡니다. 그게 이 변환기에서
  **기록이 말하지 않는 것을 적는 유일한 자리**이니 다른 스텟으로 번지게 하지 마세요.
- **구속만 실측이 아닙니다.** KBO 기록실에 구속 항목이 없어 K/9 로 대용합니다.
  README 에 명시해 두었습니다 — 지우지 마세요.

### 경기 시뮬레이션

- **한 타석의 결과는 반드시 닫혀야 합니다.** 확률 합이 1을 넘으면 아웃이 음수가 되어
  이닝이 안 끝납니다. `_probs()` 끝에서 정규화하고, 반이닝 루프에 상한을 두세요.
- **수치를 만졌으면 `--simtest` 부터.** KBO 한 팀 평균 득점은 4.5~5.5점입니다.
  현재 기준선: 팀당 4.5~5.0점 · 10~11안타. **승률은 실제 리그 격차로 보세요** —
  차이 12(리그 최대)에서 79.4% 입니다. 종합85 대 55(30 차이)의 95~98% 는 게임에
  없는 격차입니다.
  (번트·주력과 투수 정신력이 경기에 닿게 되면서 89% → 92% 로 올랐습니다 —
  스텟이 더 많이 반영될수록 센 팀이 더 확실히 이깁니다.)
- **투수 체력은 문턱이 아니라 기력 풀입니다.** 최대 기력 = **체력의 제곱**,
  공 하나당 `D.GAS_PER_PITCH`(41.5) 소모, 남은 비율의 `TIRE_POW` 제곱만큼 스텟이
  깎입니다. **한계 투구 수 문턱으로 되돌리지 마세요** — 문턱이면 그 앞까지 체력
  99 와 66 이 완전히 같아서, 체력을 올려도 뭐가 좋아졌는지 화면에 안 나타납니다.
  제곱인 것도 일부러입니다: 선형이면 마무리(38)와 에이스(92)가 1.4배뿐인데
  제곱이면 5.9배로 벌어져 "끝까지 가는 투수"가 실제로 존재하게 됩니다.
- **`GAS_PER_PITCH` 는 타석이 아니라 투구 수에 곱합니다.** 한 타석이 3~5구이므로
  타석당으로 착각해 3.7배 작게 잡으면 기력이 남아돌아 **평범한 선발도 완투 33%**
  가 나옵니다(실제로 그랬습니다).
- **결과별 투구 수(`D.PITCH_COST`)에서 삼진을 싸게 만들지 마세요.** 인플레이 아웃
  3.2구 < 삼진 4.7구 < 볼넷 5.4구입니다. 삼진이 싸지면 구위 하나만 높은 카드가
  완투까지 가져가서 제구·체력을 볼 이유가 없어집니다. **완투로 가는 길은 제구**이고,
  삼진형은 공을 아끼는 게 아니라 주자를 안 남겨서 갚습니다(실측: 제구형 완투 11.6%
  대 삼진형 4.0%).
- **수비 위치 페널티를 작게 두면 포지션이 장식이 됩니다.** `_def_of` 가 아홉 명
  **평균**을 내므로 한 명의 이탈이 9분의 1로 희석됩니다 — `OUT_OF_POS` 가 12 이던
  시절 아홉 명을 전부 엉뚱한 자리에 세워도 상대 득점이 **4.67 대 4.70(노이즈)**
  이었습니다. 지금은 계열 안 20 · 계열 밖 40 이고, 한 명 이탈이 +0.13점 ·
  전원 이탈이 +0.87점입니다.
- **`build_ai` 는 카드 포지션에 맞춰 자리를 배정해야 합니다.** 예전에는 타순대로
  포수·1루수·2루수… 를 그냥 붙여서 **AI 아홉 명이 거의 다 자리 이탈**이었습니다.
  페널티가 작을 땐 안 보였지만 40 으로 올리자 AI 실점이 **4.0 → 5.2** 로 뛰었습니다 —
  상대가 수비를 못 짜서 생기는 점수는 내 오더의 실력이 아닙니다.
- **자리를 맞출 때 "무작위로 찔러 보고 맞으면 쓰기"를 하지 마세요.** 후보가 적은
  자리(포수)에서 자주 실패해 엉뚱한 카드로 흘러가고, 그러면 `target_ov` 가 팀
  세기를 못 나타냅니다 — 실제로 **종합 차이 4 의 승률(65%)이 차이 8(62%)보다
  높아졌습니다.** 후보를 **모아 두고** 그중에서 뽑으세요.
- **삼진을 늘리는 것만으로는 실점이 안 줄어듭니다.** `p["out"]` 이 나머지(1 − 합)라
  삼진이 늘면 인플레이 아웃이 그만큼 줄어 **아웃 총량이 안 변합니다.** 밀어내기를
  막는 이득과 병살을 놓치는 손해가 상쇄되어, 구속·변화구가 삼진 확률에만 닿던
  시절 실측이 **−0.03점(노이즈)** 이었습니다 — 종합에는 둘이 0.20 이나 들어가는데
  경기에서는 없는 셈이었습니다. 지금은 피안타 억제(`hitmul`)에 네 칸이 몫을 나눠
  갖습니다: `SUP_STUFF` 0.38 · `SUP_CTL` 0.30 · `SUP_VELO` 0.16 · `SUP_BRK` 0.16.
  **합은 1.0 이어야 합니다** — 합이 바뀌면 `K_CONTACT` 의 뜻이 통째로 달라집니다.
- **투수 스텟은 투수진 전체에 걸어서 재세요.** 선발 한 명만 바꾸면 불펜이 희석해서
  값이 작게 나옵니다. 현재 실측(전체 50→85, 상대 타선 교타 78): 구위 −1.69 ·
  정신력 −1.04 · 제구 −0.76 · 체력 −0.41 · 변화구 −0.34 · 구속 −0.34.
- **타자 스텟 실측(전체 50→85)**: 교타 +0.94 · 장타 +0.88 · 수비 −0.64(상대 득점) ·
  주력 +0.62 · 번트 +0.46 · 정신력 +0.40.
- **완투는 투구 수 상한(`D.CG_PITCH_BASE` + 체력 × `D.CG_PITCH_PER`)과 `D.CG_RUNS`
  두 값이 전부입니다.** 8·9회 교체를 무조건으로 되돌리면 완투가 사라지고, 느슨하게
  두면 마무리 칸이 놉니다. 만졌으면 **선발 유형별로 재세요** — 리그 평균만 보면
  안 드러납니다.
  입니다(구위·제구 95 기준). 실제 카드로는 류현진 10 27% · 리오스 07 26% 인데
  두 사람의 실제 기록은 16% 와 6% 입니다 — 게임 최상위 두 장이라 그대로 뒀습니다.
  낮추려면 `CG_PITCH_BASE` 를 내리세요(선발이 일찍 내려가 **득점도 같이** 내려갑니다).
- **8·9회를 맡길지를 기력 비율로 가르지 마세요.** 기력이 **체력의 제곱**이라
  계단처럼 작동합니다 — 8이닝 110구 시점의 남은 비율이 체력 70 이면 6.8%,
  80 이면 28.7% 라, 문턱 25%(옛 `CG_LEFT`)를 사이에 두고 **체력 79 와 81 이
  완투 0% 대 18% 로 갈렸습니다.** 게다가 그 위로는 항상 통과라 체력을 더 올려도
  의미가 없어, 체력 92 카드가 **완투 44%** 를 찍었습니다(실제 류현진 10' 은 16%,
  리오스 07' 은 6%). 상한은 체력에 **선형**이어야 절벽이 없습니다.
- **투구 수 상한을 조이면 득점이 내려갑니다.** 선발이 일찍 내려간 만큼 **기력이
  가득 찬** 불펜이 더 던지기 때문입니다 — 상한을 125구에서 115구로 줄였을 때
  종합70 끼리의 팀당 득점이 4.4 → 3.9 로 내려갔습니다. 완투율만 보고 조이지 말고
  `--simtest` 의 득점을 같이 보세요.
- **중계는 `PULL_RUNS`(5실점) 또는 기력 고갈로만 올라옵니다.** 예전에는 교체 문턱이
  선발의 실제 등판량보다 한참 위여서 **800경기 중 5경기(0.6%)에서만** 중계가
  나왔습니다 — 오더 25칸 중 4칸이 놀고 있었던 셈입니다. 지금은 평범한 선발 기준
  42% 입니다. 여기를 만졌으면 그 비율을 다시 재세요.
- **한 번 던진 투수를 다시 올리지 마세요**(`state["used"]` · `Sim._next_arm`).
  `_new_arm` 이 기력을 0 부터 다시 세기 때문에 **기력이 통째로 회복됩니다** —
  연장에서 두 명을 번갈아 쓰면 아무도 안 지치는 상태가 됩니다. 선발도 이 목록에
  넣어야 불펜이 소진됐을 때 선발이 가득 찬 기력으로 돌아오지 않습니다.
- **연장에서 지정 투수를 갈 때는 `spent` 를 반드시 같이 보세요.** 9회에 올라온
  마무리는 이미 `used` 에 있으므로, 기력 검사 없이 "지정된 사람을 못 쓰면 다음
  사람" 으로만 짜면 **멀쩡한 마무리를 연장마다 갈아 버립니다**(실제로 연장 경기
  전부에서 교체가 일어났습니다 — 135경기에 270회). 지금은 1500경기에 35회입니다.
- **상대 팀 후보를 "목표 종합 ±n" 으로 자르지 마세요.** 시즌을 한둘만 넣어 두면 그
  폭에 아홉 명이 안 잡혀 전체에서 무작위로 뽑는 쪽으로 빠지고, 상대가 늘 평균이
  됩니다 — 종합85 팀이 종합55 팀에게 51%밖에 못 이겼습니다.
- **정신력은 주자가 있을 때만 붙입니다.** 늘 붙이면 그냥 교타력 한 칸이 됩니다.

### 화면

- **노드를 만들지 마세요.** 모든 화면을 `main.gd` 의 `_draw()` 한 곳에서 그리고,
  도감은 화면에 든 카드만 그립니다.
- **새 화면을 만들면 키 분기와 마우스 분기를 둘 다 채우고**, 자리 함수를 하나 두어
  그리기와 클릭 판정이 같은 값을 보게 하세요(`_tab_rect`·`_grid_rect`·`_slot_rect`).
- **카드는 위에서 아래로 흘려 놓습니다**(`Art.card`). 자리를 상수로 박으면 글자 크기를
  조금만 바꿔도 아래 것이 덮입니다 — COST 별이 정신력 막대를 가린 적이 있습니다.
  스텟을 늘리거나 글자를 키웠으면 `CARD_RATIO` 도 같이 키우세요.
- **JSON 에서 읽은 수는 float 입니다.** 화면에 그대로 쓰면 종합이 `99.0` 으로 나옵니다.
- 데이터는 **실행 중에** `data/players/` 에서 읽습니다. 시즌을 더 넣는다고 다시 빌드
  하지 않습니다(비트폴이 `songs/` 를 읽는 방식과 같습니다).

### 테스트 빌드 (proyamae)

`build-test.bat` → `dist\proyamae-test.exe` 는 **모든 카드가 열린 채로** 시작합니다.
오더·팀컬러·스킬블록을 손보려면 다 모은 상태가 필요한데, 실제로 모으려면 몇 시간이
걸리고 그건 그 기능을 시험하는 일과 상관이 없습니다.

- 구분은 `export_presets.cfg` 의 **`Windows Test` 프리셋**입니다 —
  `custom_features="testbuild"` 를 박아 넣고 `OS.has_feature("testbuild")` 로 읽습니다.
- **해금을 세이브에 쓰지 마세요.** 1만 장을 `owned` 에 넣으면 저장 파일이 커지고
  쓰기도 느려집니다. 플래그 하나로 `has()` · `count()` · `owned_cards()` 의 **대답만**
  바꾸면 테스트 빌드로 논 세이브를 정식 빌드로 열어도 카드가 안 늘어납니다.
- **다 가진 상태에서는 트레이드를 막습니다** — 넘긴 뒤에도 `has()` 가 true 라
  목록이 안 줄어들어 화면이 거짓말을 합니다.
- **내보낸 exe 에 인자를 줄 때는 `--` 를 먼저** 붙이세요(`OS.get_cmdline_user_args()`
  는 `--` 뒤만 읽습니다). 내보낸 빌드는 콘솔이 없어 `print` 가 안 보이므로 확인은
  캡처로 합니다.

### 빌드 성공 판정 (proyamae)

- **"exe 가 있다"로 성공을 판정하지 마세요.** Godot 은 `.tmp` 로 쓴 뒤 exe 위로
  이름을 바꾸는데, **게임이 실행 중이면 그 이름 바꾸기가 실패**합니다. 이전 빌드의
  exe 는 그대로 있으므로 `if not exist` 검사는 통과하고, `build.bat` 은 "Done" 을
  찍은 채 **옛 바이너리**를 남깁니다 — 실제로 그렇게 나갔고, 한동안 안 바뀐 exe 를
  테스트했습니다. `.tmp` 가 남았는지와 **타임스탬프가 바뀌었는지**를 같이 보세요.

### 테스트

자동화된 검사는 `check.bat`(원본) · `--simtest`(경기) · `--clicktest`(클릭) ·
`--blocktest`(스킬블록 4×4 판) · `--colortest`(팀컬러)입니다.
그 뒤 빌드 성공 + 실행으로 확인하고, 카드 수치를 만졌으면 **아는 선수의 카드가 상식에
맞는지** 눈으로 보세요(이승엽 03' 장타력, 이종범 94' 주력처럼 확실한 기준점이 있습니다).

**`convert.bat` 은 끝에 COST 별 6스텟 평균 표를 찍습니다.** `OVERALL_LIFT` · `ST_SOFT`
같은 변환 수치를 만졌으면 감으로 정하지 말고 그 표를 보세요 — 지금은 COST 1 이 40.6,
COST 10 이 81.8 입니다.

### 등급과 COST (proyamae)

- **등급은 EX 와 NORMAL 둘뿐이고, 카드에는 EX 만 표기합니다.** NORMAL 은 아무것도
  안 붙입니다 — 대부분이 NORMAL 이라 붙여 봐야 정보가 아니라 소음이고, EX 가
  눈에 안 띕니다. 등급을 다섯 단계로 되돌리지 마세요. 카드의 세기는 등급이 아니라
  **COST 와 스텟 막대**로 읽습니다.
- **경고색으로 등급 색을 빌려 쓰지 마세요**(`P.WARN` 을 쓰세요). LEGENDS 를
  없앴을 때 `P.GRADE["LEGENDS"]` 를 참조하던 곳 때문에 게임이 아예 안 떴습니다.
- **뽑기는 등급이 아니라 종합(OV) 구간으로 굴립니다**(`D.PACK_ODDS` 의 앞 숫자가
  그 구간의 최소 OV). 등급이 둘뿐이라 등급으로 뽑으면 NORMAL 하나가 99%가 되어
  1만 장에서 아무거나 나오는 것과 같아집니다.
- **COST 가 올라도 스텟이 안 오르면 카드가 다 비슷해 보입니다.** 스텟을 각 항목의
  z 로만 매기면 잘하는 선수도 특기 칸만 높고 나머지는 평균이라, COST 1 과 COST 10 이
  6칸 평균 48.5 대 60.0(한 단계당 +1.3)밖에 차이 나지 않았습니다. 그래서 종합 z 를
  각 칸에 **그 칸의 종합 비중에 비례해서** 나눠 더합니다(`OVERALL_LIFT`).
  지금은 45.2 대 68.1 이고 가장 높은 칸은 55 대 86 입니다.
- **`_to_stat` 에서 99 로 자르지 마세요.** 자른 뒤에 보정을 얹으면 상위권이 전부
  99 에 몰려 최상위 카드끼리 구분이 안 됩니다(테임즈 15' 가 교타·장타·주력 모두
  99 였습니다). 자르는 곳은 `_finish_stat` 한 곳뿐이고, 거기서 꼭대기를 눌러 폅니다.
- **스텟 폭과 경기 계수(`D.K_*`)는 한 덩어리입니다.** 폭을 두 배로 넓혔더니
  종합85 팀 승률이 77% → 96.5% 가 됐습니다. 한쪽을 만졌으면 `--simtest` 로
  다른 쪽을 다시 맞추세요.

### 시즌 운영 (proyamae)

[proyamae/src/season.gd](proyamae/src/season.gd). 리그는 **나 포함 열 팀**, 한 시즌
`D.SEASON_GAMES`(30)경기입니다. 단추 하나가 상황에 따라 시작·다음 경기·마감을 합니다.

- **내가 한 경기 칠 때 나머지 여덟 팀도 자기들끼리 붙입니다.** 내 승패만 세면 그건
  순위가 아니라 그냥 전적입니다.
- **상대 팀 로스터를 저장하지 마세요.** 이름·세기·**씨앗**만 두고 필요할 때
  `Sim.build_ai` 로 다시 만듭니다. 씨앗이 같으면 같은 팀이 나옵니다.
- **리그 세기는 시즌을 열 때 내 타순 평균 종합에 맞춥니다**(`D.LEAGUE_SPREAD`).
  고정값이면 카드를 모을수록 리그가 시시해집니다.
- **선발은 로테이션 순서대로 돕니다**(`rot_i`). 무작위로 고르면 선발 5칸이 그냥
  "아무나 한 명"이 되어 1선발과 5선발을 나눠 둘 이유가 없어집니다.
- **순위표에 무승부 칸을 빠뜨리지 마세요.** 승·패만 세면 무승부가 난 팀만 경기 수가
  모자라 보여 순위표를 못 믿게 됩니다. 승률은 KBO 와 같이 `승 ÷ (승+패)` 입니다.
- **상대 팀을 만들 때 1만 장을 매번 정렬하지 마세요.** 하루에 팀을 열 번 만드는데
  그때마다 전체 정렬을 돌리면 화면이 멎습니다. OV 내림차순으로 한 번만 정렬해 두고
  (`Sim._pool`) 목표 근처 창만 이분 탐색으로 잘라 씁니다(`Sim._window`).

### 리그 등급 · 유학 · 팀컬러 (proyamae)

- **상대의 세기는 등급이 정합니다**(`D.TIERS`). 내 로스터에 맞춰 따라오게 두면 아무리
  카드를 모아도 난이도가 그대로라 올라갈 이유가 없어집니다. `Sea.my_level()` 은
  화면 표시용으로만 남아 있습니다 — 리그 세기 계산에 다시 끌어 쓰지 마세요.
- **순위 보상은 등급이 높을수록 큽니다.** 올라가서 얻는 것이 난이도뿐이면 아무도
  안 올라갑니다.
- **승격권·강등권을 순위표에 그리세요.** 몇 등부터 올라가는지 안 보이면 목표가
  안 생깁니다.
- **유학은 COST 를 올리지 않습니다**(`D.ABROAD`). COST 상한 안에서 팀을 더 세게
  만드는 유일한 길이고 그것이 리그를 올라갈 이유입니다. 여기에 COST 를 붙이면
  유학의 존재 이유가 사라집니다. 강화를 COST 상승형으로 만들려다 되돌렸습니다.
- **경기가 안 보는 스텟을 만들지 마세요.** 카드에 숫자는 오르는데 이기는 데는 아무
  도움이 안 되는 칸은 유학지·스킬블록·종합에서 전부 거짓말이 됩니다. 예전에는
  **번트와 정신력**이 그랬습니다 — 종합에는 크게 들어가는데 경기가 안 봤습니다.
  지금은 통로가 있습니다: `D.K_LEGS`(번트+주력 → 내야안타 출루) · `D.K_PUSH`
  (번트+주력 → 밀어내기) · `D.EYE_MENTAL`(정신력 → 선구안) · `D.K_CLUTCH`
  (정신력 맞대결 → 득점권 안타·장타).
- **선구안은 볼넷·삼진과 투구 수에 같이 붙고, `Sim._eye` 하나를 같이 봅니다.**
  따로 계산하면 "볼은 잘 고르는데 공은 안 뺏는" 타자가 생겨 같은 스텟이 두 가지
  뜻을 갖습니다. **투구 수 쪽(`D.PITCH_EYE`)이 더 큽니다** — 볼넷 몇 개보다 상대
  선발을 한 이닝 일찍 끌어내리는 것이 크고, 기력이 투구 수로 도니까 그대로
  이어집니다(실측: 정신력 20 → 85 에서 상대 선발 4.01 → 3.60이닝).
- **`build_ai` 는 보직을 안 봅니다.** `rot` 을 전체 투수에서 무작위로 뽑기 때문에
  상대 선발이 체력 38짜리 마무리 카드일 수 있고, 그래서 **상대 선발 평균 이닝이
  3~4회로 나옵니다.** 이 값을 사람 팀의 선발 이닝(6이닝 안팎)과 비교하지 마세요 —
  같은 조건끼리의 **상대 비교**로만 씁니다.
- **정신력은 통로가 둘이고, 하는 일이 다릅니다.** `EYE_MENTAL` 은 **상시**로
  볼넷·삼진과 **투구 수**(선구안)에 붙고, `K_CLUTCH` 는 **득점권 한정**으로
  안타·장타에 곱합니다.
  하나로 줄이지 마세요 — 득점권에만 두면 "승부처 전용 스텟"이 되어 평소 타석에서는
  없는 것과 같습니다. **다만 세 번째를 만들지는 마세요**: 예전 `K_MENTAL` 이
  교타·장타에 몇 점 얹는 갈래였는데 +35점을 줘도 득점이 **0.01점**밖에 안 움직여서
  통째로 걷어냈습니다(약한 통로를 여러 개 두면 어느 것도 안 듣습니다).
- **`K_CLUTCH` 는 곱셈이라 세게 두면 승률이 튑니다.** 좋은 팀은 타자 정신력도
  투수 정신력도 높아서 공·수 양쪽으로 이득을 봅니다 — 0.0020 에서 종합85 대
  종합55 승률이 **94%** 였습니다. 효과 크기와 승률을 **같이** 보고 정하세요.
- **승률은 종합85 대 55(30 차이)가 아니라 실제 리그 격차로 보세요.**
  `D.LEAGUE_SPREAD` 가 최대 12 이므로 게임에서 30 차이는 나오지 않습니다.
  현재 실측: 차이 4 → 53.5% · 8 → 72.3% · **12 → 83.0%** · 30 → 96.5%.
  `--simtest` 의 96% 만 보고 "너무 일방적"이라고 판단하면 안 됩니다.
- **타순 시너지 보너스를 만들지 마세요.** "1·2번 발 빠른 선수 · 3·4·5번 거포"가
  최선인 것은 **모델이 실제 야구를 흉내 내기 때문**이어야지, 그 배치에 보너스를
  줘서가 아닙니다. 자연스럽게 그렇게 되는 장치가 셋입니다:
  ① **`bases` 는 그 루에 선 주자의 주력**입니다(-1 = 빈 루). 예전에는
  `[bool, bool, bool]` 이고 몇 베이스를 더 가느냐를 **타자의** 주력으로 정해서,
  1루 주자가 이종범이든 포수든 똑같이 갔습니다 — **타순을 어떻게 짜도 득점이
  같았습니다**(정석 3.407 대 거꾸로 3.421).
  ② **도루**(`D.STEAL_TRY`) — 1루에 주자, 2루가 비면 발로 훔칩니다.
  ③ **병살타**(`D.DP_BASE` · `D.K_DP`) — 1루에 느린 주자가 있으면 둘이 죽습니다.
  지금은 정석 3.261 · 뒤섞음 3.228 · 주자 뒤로 3.194 · 거꾸로 3.178 입니다.
- **타순 효과를 크게 만들려고 도루를 늘리지 마세요.** 시도 확률을 0.10 → 0.16 으로
  올렸더니 **뒤섞음이 1위가 됐습니다**(도루 실패가 늘어 앞 타순의 이점을 먹습니다).
  차이 2.6% 는 실제 야구의 타순 최적화 값어치(경기당 0.05~0.1점)와 같은 크기이고,
  그 이상은 야구가 아니라 다른 게임이 됩니다.
- **주자 추월을 막으세요.** `_push` 는 앞 주자(3루)부터 처리하고 `limit` 으로
  뒷주자가 앞주자를 지나가지 못하게 합니다. 주자마다 따로 굴리면서 이걸 빼면
  발 빠른 뒷주자가 느린 앞주자를 추월하는 장면이 나옵니다.
- **유학은 카드당 평생 한 번, 스텟 하나만 +`D.AB_UP`.** 리그 등급이 주는 것은 크기가
  아니라 **선택지**입니다 — 위로 갈수록 교타·장타·제구·구위 같은 중요한 스텟의
  지역이 열립니다. 등급별로 다른 상승치를 주면 낮은 등급 유학이 그냥 열등한 물건이
  되어, 올라갈 이유가 "선택지"가 아니라 "수치"로 바뀝니다.
- **유학지는 `kind` 로 갈래를 나눕니다.** `DB.find` 는 카드에 없는 스텟을 그냥 건너뛰
  므로, 안 나누면 **타자를 구위 유학에 보내고 코인과 경기만 날립니다.** 화면·클릭·키가
  `_region_ids()` 하나를 같이 봐야 합니다 — 따로 세면 타자 화면의 `[R]` 이 투수
  유학지를 가리킵니다.
- **유학 표를 고치면 `Sv.STUDY_VER` 을 올리세요.** 저장되는 것은 지역 *번호*라, 표가
  바뀌면 예전 3번(도미니카)이 지금 3번(네덜란드)이 되어 **카드가 조용히 엉뚱한 스텟을
  받은 채로 남습니다.** 화면에는 멀쩡해 보여서 눈으로는 못 잡습니다.
- **유학 기간은 경기 수이고, 그 동안 오더에 못 넣습니다**(보내면 자동으로 자리에서
  빠집니다). 코인만 내고 즉시 세지면 그냥 상점이라 선택이 아닙니다.
- **한 카드는 평생 한 번만** 갑니다(지역마다 한 번이 아닙니다). 여러 번 보낼 수
  있으면 한 장에 무한히 부어서 오더가 카드 한 장짜리가 됩니다.
- **팀컬러는 한 번에 하나만 켭니다**(`Col` · 작전 화면의 **팀컬러 탭**에서 고름). 겹쳐 쌓으면 팀컬러
  하나로 카드 등급 차이가 통째로 지워지고 무엇을 노리고 모으는지도 흐려집니다.
  하나만 켜기 때문에 값을 크게 줄 수 있습니다.
- **단일팀 · 단일연도 · 왕조 · 듀얼팀은 오더 25칸을 다 채워야 켜집니다.** 예전에는
  4·6·8명 계단이었는데, 타순 절반만 맞춰도 켜져서 "팀컬러를 노리고 모은다"가 아니라
  **어쩌다 켜지는 것**이 됐습니다. 전부 아니면 아무것도 아니게 두면 목표가 분명해지고
  그만큼 크게 줄 수 있습니다. 25는 `Col.roster_size()` 가 **`D` 의 칸 수에서 계산**합니다 —
  상수로 박으면 오더 구성이 바뀔 때 어긋납니다.
- **왕조(같은 구단 계보 · 같은 시즌)를 제일 크게 두세요**(지금 야수 +24 · 투수 +18). 그 시즌 그 구단 안에서만
  25명을 골라야 해서 낮은 코스트를 섞을 수밖에 없고, 그 손해가 큰 보너스와 맞물려
  균형이 잡힙니다. **단일연도가 단일팀보다 위**인 이유는 고를 수 있는 카드가 적기
  때문입니다 — 한 구단은 27시즌치가 있지만 한 시즌은 열 구단치뿐입니다.
- **듀얼팀의 id 는 순서를 타면 안 됩니다.** 인원이 뒤집히면 저장된 선택이 풀립니다 —
  두 계보를 정렬해서 id 를 만듭니다.
- **시대만 계단으로 남겨 두세요.** 처음 켜 보는 팀컬러가 하나는 있어야 이 장치가
  있다는 걸 알게 됩니다. 기록 · 수비 · 등급 컬러도 같은 자리입니다.
- **단일 구단 · 단일 시즌만 두지 마세요.** 기록 · 수비 · 등급으로도 열려야 "이 카드들을
  왜 같이 쓰는가"가 여러 갈래로 생깁니다.
- **전신 · 후신 구단은 한 계보로 셉니다**(해태·KIA · SK·SSG · 우리·히어로즈·넥센·키움).
  안 그러면 2000년 해태 38장이 영영 아무 데도 못 낍니다.
- **벤치는 세지 마세요** — 25칸 컬러는 벤치까지 세지만, 기록·수비 컬러는 출전하는
  자리만 셉니다. 출전 수(판의 칸·구종)도 같은 이유로 벤치는 안 셉니다.
- **`DB.cards` 의 원본 카드를 고치지 마세요.** 모두가 나눠 보는 배열이라 유학·팀컬러
  보너스를 원본에 쓰면 도감·뽑기·상대 팀에까지 번집니다. `DB.find` 와
  `Sim._tint` 는 사본을 만들어 돌려줍니다. 유학 상태가 바뀌면 `DB.clear_cache()`.
- **`DB.find` 를 선형 탐색으로 되돌리지 마세요.** 카드가 1만 장이고 `total_cost()`
  하나가 이걸 25번 부릅니다 — 매 프레임 25만 번 비교하게 됩니다. 색인(`_index`)을 씁니다.

### 스킬블록 · 구종 (proyamae)

[proyamae/src/grow.gd](proyamae/src/grow.gd).

- **출전 경기 수 하나(`Sv.games`)로 판의 칸과 구종 등급을 둘 다 굴립니다.** 성장 축을
  둘로 나누면 어느 쪽을 키우는 중인지 모르게 됩니다.
- **스킬블록은 뽑아서 모으고 카드 판에 끼우는 물건입니다**(원작이 그렇습니다).
  카드가 스킬을 타고나지 않습니다 — `Sv.blocks`(가방) · `Sv.block_at`(어느 카드
  어느 칸에 놓였나) 두 곳에 저장합니다.
- **블록은 전부 네 칸짜리입니다.** 크기를 섞으면 칸당 값이 같은 이상 "작은 것
  여럿"이 언제나 유리해서 **모양 맞추기가 개수 세기**가 됩니다. `--blocktest` 가
  회전 네 방향 모두 네 칸인지 셉니다.
- **모양은 네 가지 — `ㅁ` `l` `ㄱ` `ㄴ`.** 테트로미노 일곱 가지이던 시절에는
  **S·Z·T 가 4×4 판에서 유독 안 맞아** 좋은 블록을 뽑고도 못 끼우는 일이 잦았습니다
  (못 끼운 횟수 1145 → 635). `l`(세로)과 `ㅡ`(가로)는 같은 조각을 돌린 것이라 한
  종류이고, `ㄱ`과 `ㄴ`은 거울상이라 회전으로 안 바뀌므로 다른 종류입니다.
- **모양을 바꾸면 옛 세이브를 이관하세요**(`Sv._migrate_shapes`). 모르는 모양을
  그냥 두면 그림은 기본값으로 그려지는데 값(`SHAPE_MUL`)은 1.0 으로 떨어져
  **그림과 값이 따로 놉니다.**
- **모양마다 값이 다릅니다**(`SHAPE_MUL`). O·I 가 놓기 쉽고 S·Z 가 어려우니 어려운
  쪽을 크게 줍니다 — 안 그러면 S 가 나왔을 때 그냥 꽝입니다.
- 판은 4×4 이고 칸은 가운데 2×2 넷에서 시작해 8~110 경기에 걸쳐 열여섯까지 열립니다.
  **EX 카드는 처음부터 전부 열려 있습니다.**
- **끼우는 것은 드래그입니다.** 판 위 우클릭은 빼기, 가운데 버튼은 돌리기.
  판에 있는 것도 다시 집어 옮길 수 있어야 합니다 — 못 옮기면 판을 다시 짜려고
  전부 빼야 합니다. **놓기 전에 되는지 보여 주세요**(`Art.blocks` 의 `ghost_*`) —
  떨어뜨린 뒤에 실패 글을 읽는 것은 늦습니다.
- **자리 함수 하나(`Art.cell_rect`·`cell_at`)를 그리기 · 집기 · 놓기가 같이 봅니다.**
- **드래그만 두지 마세요.** 키보드로도 넣을 수 있어야 합니다(`Q W E R T`), 그리고
  자동으로 채우기를 손으로 하는 길 옆에 나란히 두세요.
- **화면을 넘길 때 끌던 것을 놓으세요.** 작전에서 집은 카드를 든 채로 구단관리 화면에
  가면, 거기서 뗀 것이 작전 칸 맞바꾸기로 흘러 들어갑니다(실제로 검사 셋이 깨졌습니다).
- **재화는 코인 하나입니다.** 블록도 선수 카드도 코인으로 뽑습니다 — 재화를 나눠
  두면 한쪽이 안 쓰이는 화면이 되기 쉽습니다. **대신 값을 벌려 놓으세요**
  (선수 팩 1,000 · 블록 300). 블록 하나가 스텟 +7 안팎이라 카드 한 장보다
  가벼워야 하고, 너무 싸면 선수 팩을 안 뽑게 됩니다.
- 트레이드는 **안 쓰는 카드를 코인으로 바꾸는 길**입니다. 값은 종합이 아니라
  **COST** 로 냅니다(카드의 세기를 읽는 축이 COST 라서 넘길 때도 같은 축으로
  보여야 합니다).
- **오더에 든 카드와 유학 중인 카드는 못 넘깁니다.** 넘기면 자리가 조용히 비어서,
  아홉 명이 안 차는 것을 경기가 시작되고 나서야 압니다. **마지막 한 장을 넘기면
  끼워 둔 블록이 가방으로 돌아와야** 합니다.
- **구종은 저장하지 마세요.** 카드 id 를 씨앗으로 그때그때 다시 만듭니다 —
  1만 장 분량을 세이브에 넣을 이유가 없고, 같은 카드는 언제 봐도 같은 구종이어야
  합니다. 그래서 정렬 비교자가 **id 해시**를 쓰지 무작위를 쓰지 않습니다.
- **구종 보너스는 등급 평균으로 냅니다.** 합으로 내면 구종이 많은 투수가 두 번 이득을
  봅니다(변화구가 높아 구종도 많고, 그래서 보너스도 크고).
- 유학 · 블록 · 구종은 **셋 다 `DB.find` 에서 사본에 얹힙니다.** 하나라도 원본을 고치면
  도감·뽑기·상대 팀에까지 번집니다. 상태가 바뀌면 `DB.clear_cache()`.

### 선수 사진 (proyamae)

KBO CDN 에 선수 사진이 있습니다 — `KBO_IMAGE/person/middle/<연도>/<선수번호>.jpg`
(94×118 JPEG). 선수번호는 기록실 표의 `playerId=…` 링크에서 `ids.bat` 이 받습니다.

- **사진은 2016년부터만 있습니다.** 2015 이전은 누구도 없습니다. 옛 시즌 카드는
  같은 선수의 나중 사진을 빌려 쓰고(`data/photos/index.json`), 2016 이후 등록된 적이
  없는 선수(심정수·양준혁)는 사진 없이 벡터로 갑니다.
- **없는 연도를 찾아 헤매지 마세요.** 카드 자료에 있는 (선수번호, 연도) 조합만
  시도합니다. 은퇴 선수마다 열한 해를 두드리면 요청이 수만 건이 됩니다.
- **Godot 헤드리스는 `--script` 모드에서 TLS 를 못 엽니다**("SSL module failed to
  initialize"). 그래서 `tools/photos.gd` 는 목록만 만들고 색인만 세우며, 실제
  내려받기는 `photos.bat` 안의 **curl** 이 합니다(윈도우 10 기본 탑재).
- **사진을 exe 안에 넣지 마세요.** `data/photos` 의 `.gdignore` 로 임포트를 막고
  `build.bat` 이 exe 옆으로 복사합니다. PCK 에 넣으면 실행 파일이 85MB 커집니다.
  저장소에도 안 넣습니다(`.gitignore`) — `photos.bat` 이 다시 받아 옵니다.
- **사진 폴더가 비어 있어도 게임은 돌아야 합니다**(`Spr.tex` 가 null 을 주면
  `Art._figure` 가 벡터로 그립니다). 좀비디펜스의 `art/` 와 같은 규칙입니다.
- **세로 사진을 가로 자리에 늘리지 마세요** — 높이에 맞춰 비율을 지킵니다.
- 자잘한 함정 둘: 오토로드에 `ready` 라는 변수를 두면 Node 의 것과 겹쳐 파싱이
  깨지고, `_get` 이라는 함수는 Object 의 가상 함수와 서명이 안 맞아 거부됩니다.

### 클릭이 안 먹을 때 (proyamae)

- **루트 `Control` 의 `mouse_filter` 를 IGNORE 로 두세요.** 기본값 `STOP` 이면 그
  노드가 마우스 이벤트를 GUI 입력으로 먼저 삼켜서 `_unhandled_input` 까지 오지
  않습니다 — **키보드는 멀쩡한데 클릭만 통째로 안 먹고**, 화면은 아무 이상 없어
  보여서 눈으로는 못 잡습니다. `main.gd` 의 `_ready()` 에서 설정합니다.
  (자식 Control 을 안 쓰고 좌표로 직접 판정하므로 GUI 처리가 필요 없습니다.)
- **`run.bat -- --clicktest` 를 화면 추가할 때마다 늘리세요.** 화면마다 단추·칸·
  목록을 실제로 눌러 보고 상태가 바뀌었는지 봅니다. 실패가 있으면 종료 코드 1.
  이 검사가 없었으면 위 버그를 계속 몰랐을 것입니다.
- **클릭만 검사하지 마세요 — 키도 눌러 보세요**(`_press`). 클릭이 멀쩡한데 키만
  다른 길로 새는 버그는 화면을 봐도 안 보입니다. 스카우트의 `Enter` 가 언제나
  선수 팩을 뽑아서 "코인이 모자랍니다"가 뜨던 것이 그렇게 숨어 있었습니다.
- **고친 뒤에는 되돌려서 검사가 실제로 잡는지 보세요.** 통과하는 검사를 새로
  넣는 것은 쉽고, 그게 아무것도 안 잡는 검사인지는 그때만 알 수 있습니다.
- **입력은 다음 프레임에 처리됩니다.** `Input.parse_input_event` 로 시험할 때 누른
  직후에 상태를 읽으면 안 바뀐 것처럼 보입니다 — 몇 프레임 뒤에 읽으세요.

### 카드 목록과 큰 화면 (proyamae)

- **목록에는 작은 카드만 그립니다**(`Art.small_card`) — 사진 · `15' 테임즈` ·
  `1루수 ●10`. 스텟 막대 여섯 줄을 수백 장 깔면 어느 카드가 센지 오히려 안 읽히고
  그리기 호출도 카드당 스무 번씩 듭니다. 자세한 것은 큰 화면에서.
- **큰 화면은 어느 화면에서나 우클릭으로 엽니다** — 도감 · 작전(배치선수·보유선수
  둘 다) · 구단관리 격자. **예외를 두지 마세요**: 화면마다 우클릭의 뜻이 다르면
  매번 헷갈립니다(작전에서 잠깐 자리 비우기로 뒀다가 되돌렸습니다 — 지금은
  더블클릭이 그 일을 합니다). 통로는 `_card_at()` 하나입니다. 화면마다 따로 분기를
  두면 한 곳을 빠뜨렸을 때 "여기선 안 되네"가 되고, 그게 눈에 안 띕니다.
- **큰 화면은 한 장에 다 보여 줍니다** — 카드(스텟 + 보너스) · 프로필 · 구종 방사도
  또는 수비 위치 · 가로 기록 표 · 4×4 스킬블록 판 · 추가 능력치. **뒤집기를 두지
  마세요**: 구종과 블록을 보려고 매번 사진을 눌러야 해서 "지금 뭘 보고 있는지"를
  놓칩니다(한때 앞뒤 두 면이었는데 걷어냈습니다).
- **기록은 가로 표로.** 세로로 늘어놓으면 자리를 많이 먹고, 기록은 항목 이름보다
  숫자를 나란히 보는 것이 읽기 쉽습니다.
- **프로필에 없는 값을 지어내지 마세요.** 원작에는 나이·혈액형·출신교가 있지만
  KBO 기록실에는 없습니다 — 채워 넣으면 "실제 기록으로 만든 카드"라는 전제가
  깨집니다. 아래 기록 표에 있는 값도 프로필에 또 적지 않습니다.
- **모달 높이는 내용에 맞추세요.** 넉넉하게 잡으면 아래가 통째로 빕니다.
- **안 가진 카드는 회색으로 그립니다**(`Art.small_card` 의 `dim`). 도감이 1만 장이라
  "무엇을 모았나"가 한눈에 안 보이면 도감이 그냥 카드 사전이 됩니다. **사진까지**
  덮어야 갈립니다 — 다 그린 위에 어두운 막을 한 겹 덮으세요.
- **`Art.mini` 라는 이름을 쓰지 마세요** — Godot 내장 전역 `mini(int, int)` 와 겹쳐
  조용히 그쪽이 불립니다(`small_card`). 같은 이유로 `_get`·`ready` 도 피합니다.

### 추가 능력치 표기 (proyamae)

- 유학 · 스킬블록 · 구종 · 팀컬러로 오른 몫을 스텟 옆에 **`+N`** 으로 적고, 막대의
  **원래 값까지를 흐린 색**으로 칠합니다.
- **실제로 오른 만큼만 적으세요.** `DB.find` 가 보너스를 얹을 때 99 에서 잘린 몫을
  빼고 `up` 에 남깁니다 — 잘린 몫까지 `+N` 이라고 쓰면 **옆에 적힌 숫자와 안 맞아서
  보너스가 거짓말이 됩니다.** 갈래별 원래 값은 `up_src` 에 따로 두고 "99 를 넘지
  않는 만큼만 붙는다"를 화면에 같이 적습니다.
- **팀컬러는 카드가 아니라 팀에 붙습니다**(`Sim._tint`). `DB.find` 에 넣지 마세요 —
  도감·뽑기·상대 팀에까지 번집니다. 화면에서만 `Sim.color_bonus()` 로 더해 보여 줍니다.
- **수비 위치 표시는 다이아몬드 기준**(`Art.FIELD_AT` — 내야 한 변이 1)입니다.
  패널 비율(0~1)로 잡으면 판이 가로로 넓어질 때 다이아몬드는 가운데 작게 그려지는데
  표시만 비율대로 퍼져서 **그림 밖으로 나갑니다**(실제로 그랬습니다).

### 거르개는 콤보박스입니다 (proyamae)

- 누르면 목록이 펼쳐지고 하나를 고르면 닫힙니다. **`◀ ▶` 로 하나씩 넘기지 마세요** —
  구단 11개 · 시즌 27개 · 포지션 13개짜리 거르개에서는 원하는 값까지 스무 번을
  눌러야 합니다. 지금 거르개는 시즌 · 구단 · 포지션 · 코스트 · 등급 · 종류 여섯입니다.
- **펼친 목록은 맨 나중에 그리세요**(`_draw_combo_open`). 아래 칩과 카드를 덮어야
  하는데, 칩 그리는 자리에서 같이 그리면 다음 칩이 그 위에 얹힙니다.
- **클릭도 펼친 목록부터 보세요.** 칩을 먼저 보면 목록에서 고른 것이 그 아래 칩의
  클릭으로 새어 나갑니다.
- 도감 거르개는 여섯(시즌·구단·포지션·코스트·등급·종류), 작전 보유선수 거르개는 넷
  (구단·시즌·포지션·코스트)이고 보유선수 판 위에 가로로 늘어놓습니다.
- 자리 함수는 `_chip_rect` · `_combo_item_rect` · `_combo_panel`(도감)과
  `_pool_*`(작전 보유선수)이고 **그리기와 클릭이 같이 봅니다.** 펼침 상태(`combo_i`)는
  둘이 같이 쓰므로 **화면을 넘길 때 반드시 닫으세요**(`_go` 가 합니다).

### 스카우트 — 팩을 카드로 늘어놓습니다 (proyamae)

- 팩마다 한 칸입니다(왼쪽 팩 그림 · 오른쪽 이름/내용/값). **단추 두 개만 있으면
  "무엇을 뽑는 것인지"가 안 읽힙니다.**
- **팩 전체가 눌립니다.** 작은 단추만 눌리면 "어디를 눌러야 하나"를 찾게 됩니다.
- **한 팩에 선수 카드 10장**(`D.PACK_SIZE`). 시작 코인(`Sv.START_COINS`)은 그에
  맞춰 잡습니다 — 팩이 커지면 한 판에 필요한 코인도 같이 커집니다.
- **열 장은 한 줄에 안 들어갑니다.** 다섯씩 두 줄로 깔되 **폭과 높이를 둘 다 보고**
  카드 크기를 줄이세요. 폭만 보면 두 번째 줄이 화면 아래로 넘칩니다.
- **알림(`toast`)은 화면 위쪽에 띄웁니다.** 아래에 두면 카드가 화면을 가득 채우는
  스카우트에서 결과 위에 얹힙니다.
- **팩이 둘이면 키로도 고를 수 있어야 합니다**(`←→` · `Enter`). `Enter` 가 언제나
  첫 팩을 뽑게 두면, 블록을 뽑으려 해도 선수 팩 쪽으로 가서 엉뚱한 값을 검사합니다 —
  실제로 그렇게 나갔고, **클릭은 멀쩡한데 키만 새는** 종류라 화면을 봐도 안 보였습니다.
  클릭과 키가 `_draw_pack_now()` **하나를 같이 지나게** 두세요.

### 구단관리 — 왼쪽 카드 격자, 오른쪽 선수 설명 (proyamae)

- 왼쪽은 **카드 격자**입니다. 한 줄짜리 목록으로 두면 카드가 수백 장이 됐을 때
  이름만 늘어놓은 표가 되어 **어느 카드가 센지 눈으로 못 고릅니다.**
- **유학 상태를 카드 위에 겹쳐 쓰세요** — 나가 있으면 어둡게 덮고 남은 경기를,
  다녀왔으면 횟수를 찍습니다. 격자에서 바로 안 보이면 작전을 짜다 없는 선수를
  찾게 됩니다.
- **오른쪽 판의 내용은 전부 `_study_side()` 를 기준으로 잡으세요.** 본문 왼쪽에서
  상수로 밀면(예전 `b.position.x + 452`) 카드 격자 폭이 바뀔 때 판이 격자 위로
  올라탑니다 — 실제로 그렇게 겹쳤습니다. 갈래별 내용은 **정보 줄 아래에서**
  시작해야 마지막 줄과 안 겹칩니다.
- **되돌릴 수 없는 것은 한 번 물어봅니다.** 유학은 코인이 나가고 그 카드가 여러
  경기 동안 작전에서 빠지는데 되돌릴 방법이 없습니다 — `선수 유학결정` 창에
  코스트 · 등급 · 유학지 · 오르는 스텟 · 기간 · 비용을 세우고 확인을 받습니다.
  **키로도 같은 창을 거쳐야 합니다** — 마우스와 다른 길로 보내지면 실수로 눌렀을
  때 되돌릴 방법이 키 쪽에만 없게 됩니다.
- 못 가는 지역은 **그 자리에 이유를 적으세요.** 눌러 보고 나서야 아는 것보다
  무엇을 채우면 열리는지 미리 보이는 쪽이 낫습니다.

### 화면은 탭이 아니라 페이지입니다 (proyamae)

홈에서 골라 들어가고 왼쪽 위 `◀` 또는 `Esc` 로 돌아옵니다(원작 구조).
**탭 줄을 늘 띄워 두면 화면마다 쓸 수 있는 세로 폭이 그만큼 줄어듭니다** —
작전처럼 위아래 두 판을 쌓아야 하는 화면이 눌립니다.

- **화면 전환은 `_go()` 하나를 지납니다.** 끌던 것(`drag_uid`·`drag_slot`)과
  펼친 것(`combo_i`·`pos_open`)을 거기서 전부 닫습니다 — 안 그러면 작전에서 집은
  카드를 든 채로 다른 화면에 가서, 거기서 뗀 것이 엉뚱한 곳으로 흘러갑니다
  (실제로 그렇게 나가서 검사 셋이 깨졌습니다).
- `1`~`5` 는 홈을 거치지 않는 지름길입니다. `--screen=<이름>` 도 같은 표를 씁니다.

### 작전 — 위 배치선수, 아래 보유선수 (proyamae)

원작 작전 화면과 같은 구조입니다. 예전에는 칸을 누르면 **고르기 창**이 떴는데,
아홉 자리를 채우는 데 창을 아홉 번 여닫아야 했습니다. **목록을 늘 띄워 두면
칸을 고르고 아래에서 누르는 것만으로** 채워집니다.

- **넣으면 다음 빈 칸으로 옮깁니다**(`_next_empty`). 스물다섯 칸을 채우는데 칸을
  매번 손으로 옮겨야 하면 목록을 늘 띄워 둔 뜻이 없습니다.
- **보유선수 목록에 이미 오더에 든 카드를 보여 주지 마세요.** 눌렀을 때 아무 일도
  안 일어나서 목록을 믿을 수 없게 됩니다. 유학 중인 카드도 뺍니다.
  오더가 바뀌는 모든 곳에서 `_refresh_pool()` 을 부릅니다.
- **든 칸은 끌기, 빈 칸은 고르기만**입니다. 칸끼리 놓으면 맞바뀌고, **타순끼리면
  수비 위치도 같이** 따라갑니다 — 자리는 그대로 두고 사람만 바꾸면 3루수 자리에
  포수가 섭니다. **같은 탭 안에서만** 바꿉니다(타자 칸에 투수가 들어가면 경기가
  안 되고, 그걸 경기 시작하고서야 압니다). 위아래 키도 지금 탭 안에서만 돕니다.
- **COST 총합은 탭과 상관없이 25칸 전부를 셉니다.** 탭 안의 것만 세면 투수 탭에서
  상한을 넘긴 줄 모르고 경기에 들어갑니다.
- **수비 위치는 카드 아래 드롭다운에서 고릅니다**(`_pos_rect` · `_pos_item_rect` ·
  `_pos_panel`). 예전에는 `,` `.` 키뿐이라 **마우스로는 못 바꿨습니다** — 자리
  이름이 화면에 멀쩡히 찍혀 있어서 눈치채기 어려웠습니다. 키(`P` · `,` `.`)도
  같이 남기세요. 자리는 **칸 밑변에서 잡습니다** — 칸 높이를 상수로 다시 쓰면
  칸이 커질 때 자리 표시만 제자리에 남아 판 테두리에 걸칩니다.
- **덮고 있는 것부터 클릭을 보세요** — 팀컬러 창 → 수비 위치 목록 → 거르개 목록
  → 칸 → 보유선수. 아래 것을 먼저 보면 위에 뜬 목록에서 고른 것이 그대로 새어
  나갑니다. 그리기는 정확히 그 **반대 순서**입니다.
- **팀컬러는 단추 하나로 엽니다.** 늘 띄워 두면 분석 판이 들어갈 자리가 없습니다.

### 균형을 재는 도구 (proyamae)

감으로 답하면 안 되는 질문들이라 도구를 두었습니다.

- `--deckscan` — 단일구단 · 단일연도 · 왕조로 짤 수 있는 **최대 코스트**. 그 풀이
  얼마나 두꺼운지를 재는 자입니다(실측: 단일구단 248 · 단일연도 244 · **왕조 193**).
- `--deckfight` — 코스트 제한 없이 세 갈래의 최강 덱을 뽑아 **서로 붙입니다.**
  홈·원정을 번갈아 해야 자리 이점이 안 섞입니다.
- **`Sim.play(away, home)` 이고 `score[0]` 이 원정입니다.** 여기를 뒤집으면 순위가
  통째로 거꾸로 나옵니다 — 실제로 평균 OV 50 짜리 덱이 84 짜리를 제치고 1등으로
  올라왔습니다. 이런 도구를 쓸 때는 **결과가 상식에 맞는지 먼저 보세요.**
- **덱을 짤 때는 상한 안에서 시작해 올리세요.** 좋은 것부터 담고 나중에 깎으면
  못 깎는 자리 하나에서 막혀 상한을 넘긴 채로 끝납니다(226~233 이 나왔습니다).

### 팀컬러 페이지 (proyamae)

- **못 켠 것도 보여 주세요.** 조건을 못 맞췄을 때 "왜 안 켜지는지"를 알 방법이
  없으면 게임이 고장 난 것처럼 보입니다 — 25칸을 다 채웠다고 생각했는데 아무것도
  안 나오면 더 그렇습니다. 실제로 그렇게 막혔습니다.
- **`Col.survey()` 가 모든 팀컬러를 진행도(`have`/`need`/`ok`)와 함께 만들고,
  `Col.active()` 는 그중 `ok` 인 것만 거릅니다.** 둘을 따로 만들지 마세요 —
  화면에 보이는 조건과 실제로 붙는 조건이 조용히 갈라집니다.
- **못 켜는 것을 누르면 이유를 말하세요**(`한화 단일팀 — 아직 3 / 25명`).
  아무 반응이 없으면 고장으로 보입니다.
- 구단·시즌은 **가장 많이 모인 것 하나씩만** 내놓습니다. 열 구단 × 27시즌을 다
  늘어놓으면 목록이 수백 줄이 됩니다.
- **`_top()` 이 열쇠를 문자열로 바꾸지 않게 하세요** — 시즌·시대 사전은 정수로 키를
  잡고 있어서, 문자열로 돌려주면 되돌아온 값으로 조회가 깨집니다(실제로 그랬습니다).
- **`--colortest` 는 네 배치를 다 봅니다** — 한 구단+한 시즌 25명(왕조·단일팀·
  단일연도) · 24명(전부 꺼짐) · 한 구단 여러 시즌(단일팀만) · 한 시즌 여러 구단
  (단일연도만) · 두 구단 절반씩(듀얼팀만). **"꺼져야" 쪽이 특히 중요합니다** —
  켜지는 것만 보면 조건을 느슨하게 만들어도 통과합니다.
- **검사 표본은 종류별 인원을 채워야 합니다**(타자 14 · 투수 11). 시즌·구단을
  흩느라 투수가 모자라면 오더에 빈 칸이 남고, 그러면 25명 조건이 안 서서
  **게임이 아니라 검사가** 틀립니다.

### 화면에 쓰는 목록을 "그릴 때만" 채우지 마세요 (proyamae)

**"25명을 맞췄는데 팀컬러가 안 나온다"가 이것 때문에 났습니다.** `color_list` 를
팀컬러 창을 그리는 함수 안에서만 채우고 있었는데, 창을 한 번도 안 열면 목록이 빈
채로 남아서 단추가 "켤 수 있는 것이 없습니다"라고 말했습니다 — 컬러는 **이미
켜져 있었는데도** 말입니다.

그리는 함수 안에서 상태를 만들면 **그 함수가 안 불린 프레임에는 그 상태가 거짓**
입니다. 화면 함수(`_draw_order` 등) 맨 위에서 갱신하거나, 아예 부를 때마다 계산하세요.

### 분석 — 타순 분석 · 배터리 분석 (proyamae)

자리마다 막대 하나를 세워 **어디가 약한가**를 보여 줍니다. 숫자만 늘어놓으면
스물다섯 칸에서 약한 자리를 못 찾습니다.

- 타자 탭은 타순 분석(1~9번), 투수 탭은 배터리 분석(선1~5 · 중1~4 · 셋 · 마).
  **벤치는 안 넣습니다** — 경기에 안 나오는 자리라 막대가 낮아도 약한 게 아닙니다.
- **종합(OV)을 그대로 쓰지 마세요.** 체력처럼 "역할을 가르는 표시"까지 섞여서
  분석이 경기 결과와 따로 놉니다. `_anal_value` 는 경기에 실제로 쓰이는 스텟만
  가중합니다.
- **빈 칸은 경고색 밑동**으로 둡니다. 0 짜리 막대로 그리면 "약한 선수"와
  "아무도 없음"이 같아 보입니다.
- **띠(나쁨·보통·좋음)는 눈에 보여야** 막대 높이가 뜻을 갖습니다. 바탕이 어두워서
  반투명 검정으로는 아무것도 안 보였습니다 — 밝기로 나눕니다.

### 지명타자 · 수비 위치 (proyamae)

- **지명타자는 카드의 포지션이 아닙니다.** "수비를 안 한다"는 뜻이라 **타순의 한
  자리**이지 카드가 타고나는 것이 아닙니다 — 지명타자 카드라는 것은 없습니다.
  변환기에서 수비 기록을 한 시즌도 못 찾은 선수는 **1루수**로 둡니다(`POS_HARD` 가
  제일 낮은 자리라 "수비 기록이 없다"와 가장 가깝습니다).
  `D.POS` 에는 지명타자가 그대로 있습니다 — 타순 아홉 자리 중 하나이기 때문입니다.
- **수비 위치는 유일합니다.** 아홉 자리를 아홉 타순이 하나씩 나눠 가집니다 —
  같은 자리에 둘을 세우면 한 자리가 비고, 그 사실이 화면에 안 드러납니다.
  **막지 말고 맞바꾸세요**(`_set_pos`). 거절하면 자리를 바꾸려고 두 번 눌러야
  하는데, 맞바꾸면 한 번에 끝나고 아홉 자리가 언제나 온전히 채워집니다.
  목록에 **`↔3번`** 처럼 누구와 바뀌는지 적으세요.

### 같은 선수는 한 명만 (proyamae)

- **이름이 같으면 보유선수 목록에서 뺍니다.** `03' 이승엽` 을 넣으면 `02' 이승엽`
  도 사라집니다 — 한 사람이 타순에 두 번 설 수는 없습니다. **목록에 남겨 두고
  거절하지 마세요**: 눌러 보고 나서야 왜 안 되는지 알게 됩니다. 넣는 쪽에도 같은
  검사를 한 번 더 둡니다(목록이 낡은 상태로 눌리는 경우).
- 이름을 모을 때는 **투수·야수를 가리지 않고 25칸 전부**를 봅니다.
- **규칙을 넣기 전에 짜 둔 오더를 조용히 고치지 마세요.** 남의 오더에서 선수를
  빼는 것은 되돌릴 수 없고 지운 사실도 안 보입니다. `중복` 표시와 경고 테두리를
  그리고 머리글에 몇 명인지 적어 **직접 고치게** 합니다.

### COST 는 6칸 합의 백분위입니다 (proyamae)

- **자르는 기준은 종합(OV)이 아니라 6칸 합입니다**(`_stat_sum`). OV 로 자르면
  같은 COST 안에서 스텟 합이 크게 벌어집니다 — OV 는 가중합인데(교타 0.30 ·
  번트 0.04) 화면의 막대 여섯 개는 균등하기 때문입니다. 실제로 COST 8 · 1루수
  안에 이승엽 17'(6칸 평균 58.2)과 나주환 16'(68.5)이 같이 있었고, 벌어진 몫의
  대부분이 번트 37 대 66 이었습니다. 합으로 자른 뒤 같은 COST 안의 6칸 평균 폭이
  **14~21 에서 3.8~7.0 으로** 줄었고, COST 8 은 어느 포지션에서나 정확히 6.3 입니다.
- **맞바꿈을 알고 쓰세요.** COST 가 더 이상 경기에서의 세기를 그대로 뜻하지
  않습니다 — 교타·장타에 몰린 카드는 합이 같아도 실제로 더 셉니다(실측 교타 +35
  가 +0.95점, 번트 +35 가 +0.14점). 되돌리려면 `_recost` 에서 `_stat_sum` 대신
  `ov` 를 넣으면 됩니다.
- **OV 를 선형으로 자르지 마세요**(옛 방식의 교훈 — 지금도 유효합니다). 분포가
  가운데 몰린 산 모양이라 `(ov-45)/5` 같은 식은 **절반을 COST 1** 로 만듭니다.
  그러면 **COST 상한이 아무것도 제약하지 않아서**, 왕조·단일팀처럼 좁은 풀에서
  골라야 하는 덱의 손해가 사라집니다 — 그 손해가 큰 보너스와 맞물리는 것이
  팀컬러 균형의 전부인데 말입니다.
- `_recost()` 가 **모든 시즌을 다 쓴 뒤 한 번** 돌면서 `COST_SHARE`
  (`[4,8,13,17,18,15,10,7,5,3]`) 대로 자릅니다 — 가운데가 두꺼운 종 모양입니다.
- **맨 위와 맨 아래 구간은 열려 있습니다.** COST 10 은 합 458 위가 전부라 폭이
  105(6칸 평균 17.5)로 혼자 넓습니다 — 23%가 합 500 을 넘고 최대가 563 입니다.
  좁히려면 `COST_SHARE` 에 칸을 더해야 하는데 `D.COST_CAP`(190)의 뜻이 같이
  움직이므로, 만졌으면 `--deckscan` 을 다시 돌리세요.
- **7코부터 카드에 테두리를 한 겹 더 두릅니다**(`D.COST_HIGH`). 6칸 평균이 6코
  52.6 → 7코 59.2 로 눈에 띄게 뛰는 자리라 그 경계가 목록에서 바로 읽혀야
  합니다. **등급(EX)의 노란 띠와 색이 겹치지 않게** 고르세요.
- **한 해만 변환해도 전체를 다시 읽어 같은 자를 씁니다.** 시즌마다 다른 기준으로
  자르면 같은 실력의 선수가 해마다 다른 COST 를 받습니다.
- **문턱은 등수가 아니라 합의 값**입니다. 등수로 자르면 스텟이 똑같은 두 카드가
  다른 COST 를 받습니다. 겹치면 뒤로 밀어 순증하게 만드세요.
- **상한은 `D.COST_CAP` = 190** 이고, 실제로 물리는지는 `--deckscan` 으로 봅니다 —
  지금 단일구단 247 · 단일연도 243 · **왕조 137~176** 입니다. 단일구단·단일연도는
  상한에 걸리고, **왕조는 상한에 아예 못 닿습니다**(176 < 190). 그 손해가 왕조의
  구조적 손해이고, 제일 큰 팀컬러 보너스가 그것과 맞물립니다. 상한이나
  `COST_SHARE` 를 만졌으면 이 세 값을 다시 재세요.

### 스텟 상한은 둘입니다 (proyamae)

- **카드 자체는 99**(`convert.gd` 의 `_finish_stat` — 실제 기록을 환산한 값),
  **보너스를 얹은 뒤는 130**(`D.STAT_MAX` — `DB.find` · `Sim._tint`).
- **얹은 뒤를 99 로 두지 마세요.** 잘 키운 카드일수록 유학·스킬블록·구종·팀컬러가
  통째로 잘려서 **"키워도 안 오르는"** 상태가 됩니다.
- **막대도 상한 기준으로 그리세요** — 99 로 나누면 보너스 받은 카드의 막대가 칸
  밖으로 삐져나갑니다.
- **색은 99 에서 최고색에 닿고 그 위는 그대로**(`P.bar`). 130 까지 늘려 펴면
  99 짜리 카드가 덜 세 보여서 카드 자체의 세기를 읽기 어려워집니다.

### 포지션 표기 (proyamae)

- **화면에 찍히는 포지션은 한글입니다**(`D.POS_SHORT`). `3B`·`CF` 가 섞이면 투수의
  `선발`·`중계`·`마무리` 와 표기 언어가 갈려 한 화면에서 따로 놉니다.
  카드·목록·큰 화면이 다 이 표를 보므로 **바꾸려면 거기만 고치면 됩니다.**
  (`D.POS_GROUP` 의 `IF`·`OF` 는 자리 이탈 판정용 내부 값 — 화면에 안 나옵니다.)
- **`--clicktest` 는 시작할 때 세이브를 비웁니다.** 카드를 뽑고 시즌을 시작하는
  검사라 안 비우면 두 번째 판이 다른 상태에서 시작해 결과가 달라집니다(실제로
  이어서 돌렸더니 한 항목이 실패했습니다). 되풀이해도 같은 결과가 나와야 검사입니다.
