# 좀비디펜스 (Zombie Defense)

**Godot 4 / GDScript** 로 만든 뱀서라이크(생존 액션)입니다.
`dist\zombie-defense.exe` **한 파일**만 있으면 실행됩니다.

- 캐릭터 그림은 [art/](art/) 의 PNG 를 씁니다. **파일이 없으면 코드로 그린 벡터로 자동 대체**되므로
  한 장만 바꿔도 되고 통째로 지워도 게임은 돕니다. 그림은 빌드 때 exe 안으로 들어갑니다.
- 소리는 파일 없이 실시간 합성합니다 ([src/snd.gd](src/snd.gd))

## 조작

| 입력 | 동작 |
|---|---|
| `WASD` / 방향키 | 이동 — **공격은 자동**입니다 |
| `1` `2` `3` | 레벨업 카드 고르기 |
| `←` `→` + `Enter` | 카드 고르기 (마우스 클릭도 됩니다) |
| `Esc` | 일시정지 / 처음 화면 |
| `M` | 음소거 |
| `Space` | 시작 · 다시 하기 |

## 한 판의 흐름

1. 사방에서 좀비가 몰려옵니다. 무기는 알아서 나갑니다 — 플레이어는 **이동만** 합니다.
2. 좀비를 잡으면 **경험치 젬**이 떨어집니다. 가까이 가면 빨려 들어옵니다.
3. 레벨이 오를 때마다 **카드 3장 중 하나**를 고릅니다 — 새 무기, 무기 강화, 보조 장비 중에서.
4. 무기를 **5레벨**까지 올리고 짝이 되는 보조 장비를 **3레벨**로 맞추면 **진화 카드**가 뜹니다.
5. **2분마다 보스**(변이체)가 옵니다. 잡으면 경험치 젬을 무더기로 떨굽니다.
6. **15분**을 버티면 작전 성공. 체력이 0이 되면 **한 번은 부활**하고, 그다음엔 실패입니다.

## 무기와 진화

무기는 최대 6종까지 들 수 있습니다. 각 무기는 **짝이 정해진 보조 장비**가 있고,
그 조합을 갖추면 완전히 다른 형태로 진화합니다.

| 무기 | 하는 일 | 짝 | 진화 |
|---|---|---|---|
| 강철 부메랑 | 가까운 적에게 관통 표창 | 탄창 확장 | **사신의 낫** — 무한 관통 |
| 볼트 런처 | 무작위 적에게 번개, 연쇄 | 강화 코어 | **뇌신의 심판** |
| 회전 사슬 | 몸 주위를 도는 쇳덩이 | 부스터 | **폭풍의 사슬** — 두 겹 |
| 포스 필드 | 주기적으로 주변을 밀어냄 | 방탄복 | **절대 방벽** — 상시 전개 |
| 레이저 런처 | 직선 관통 광선 | 조준 장치 | **프리즘 빔** — 다중 발사 |
| 드론 | 따라다니며 자동 사격 | 응급 키트 | **수호 드론단** — 5기 |
| 화염병 | 바닥에 불장판 | 에너지 큐브 | **지옥불** |
| 유도 미사일 | 쫓아가 터지는 미사일 | 자석 | **융단 폭격** |

보조 장비는 8종(에너지 큐브 · 부스터 · 자석 · 방탄복 · 강화 코어 · 응급 키트 ·
탄창 확장 · 조준 장치)이고 최대 6종까지 듭니다.

> 진화 조건을 갖추면 **진화 카드가 항상 맨 앞에 끼워집니다.** 만렙까지 올리고 짝까지
> 맞췄는데 카드 운이 나빠 안 뜨면 그 판의 목표가 사라지기 때문입니다.

## 적

| 적 | 특징 |
|---|---|
| 좀비 | 기본. 느리고 약하지만 수가 많습니다 |
| 질주 좀비 | 두 배 빠릅니다. 체력은 낮습니다 |
| 비대 좀비 | 느리지만 단단하고 아픕니다. 체력 띠가 붙습니다 |
| 폭탄 좀비 | 죽으면 터집니다 — **주변 적도 같이** 휘말립니다 |
| 침 뱉는 좀비 | 거리를 두고 원거리 공격을 합니다 |
| 변이체 (보스) | 2분마다 등장. 체력이 매우 높고 젬을 10개 떨굽니다 |

적 체력은 시간이 갈수록 오릅니다(`D.hp_scale`). 15분 시점엔 초반의 약 12배입니다.

## 빌드

```
build.bat
```

`dist\zombie-defense.exe` 하나가 생깁니다 (약 104MB — 엔진이 안에 들어 있습니다).

Godot 실행 파일은 `C:\tools\godot\Godot_v4.7.1-stable_win64_console.exe` 를 기본으로 찾습니다.
다른 곳에 있으면 환경 변수 `GODOT_BIN` 으로 지정하세요.
내보내기 템플릿(4.7.1)이 설치돼 있어야 합니다.

**단일 exe 는 `export_presets.cfg` 의 `binary_format/embed_pck=true` 로 유지됩니다.** 끄지 마세요.

## 소스 구성

| 파일 | 내용 |
|---|---|
| `project.godot` | 창 크기 · HDR 2D · 오토로드 |
| `main.tscn` | 메인 씬 + 블룸용 `WorldEnvironment` |
| `src/data.gd` | **모든 수치** — 적 · 무기 · 보조 장비 · 웨이브 · 경험치 곡선 |
| `src/game.gd` | 플레이어 상태와 성장 (레벨 · 보유 장비 · 카드 뽑기 · 진화 판정) |
| `src/world.gd` | 실시간 시뮬레이션 — 스폰 · 이동 · 충돌 · 무기 발사 |
| `src/main.gd` | 게임 루프 · 입력 · 카메라 · 상태 기계 · 그리기 |
| `src/art.gd` | 플레이어 · 적 · 투사체 · 장판 벡터 아트 |
| `src/hud.gd` | 상단 바 · 보유 장비 · 레벨업 카드 · 결과 화면 |
| `src/fx.gd` | 파티클 · 데미지 숫자 · 고리 · 섬광 |
| `src/snd.gd` | 실시간 신디사이저 (**오토로드**) |
| `src/sprites.gd` | `art/*.png` 로더 — 없으면 벡터로 대체 |
| `src/g2.gd` | 도형 · 빛 · 글자 · 이징 헬퍼 |
| `src/pal.gd` | 색 팔레트 · 폰트 · 숫자 서식 |

## 만들면서 알게 된 것

### 적을 죽일 때 배열에서 바로 빼면 안 된다

충돌 판정은 매 프레임 다시 만드는 **격자**(`World._grid`)에 담긴 **인덱스**로 돕니다.
`damage()` 안에서 `enemies.remove_at(i)` 를 해 버리면 그 뒤 인덱스가 전부 한 칸씩 밀려
엉뚱한 적을 때리거나 범위를 벗어납니다. 실제로 이것 때문에 적이 안 죽는 것처럼 보였습니다.

지금은 **`dead` 표시만 남기고** 프레임 끝의 `_sweep()` 이 한 번에 정리합니다.
모든 판정 루프는 `dead` 인 적을 건너뜁니다.

### 전수 비교로는 안 된다

적 300마리 × 투사체 200발이면 프레임당 6만 번 비교입니다. GDScript 로는 무리입니다.
84px 격자를 매 프레임 다시 만들고 **주변 3×3 칸만** 훑습니다.
적끼리 겹침을 미는 것도 **자기 칸 안에서만** 봅니다 — 정확하지 않아도 보기엔 충분합니다.

### 멀리서 죽인 젬은 영영 안 들어온다

수집 범위를 벗어난 젬은 그 자리에 남습니다. 원거리 무기가 붙으면 화면이 젬으로 뒤덮이는데
경험치는 안 오릅니다. 그래서 범위 밖 젬도 **아주 천천히(52px/s) 따라오게** 했습니다.

### 노드를 쓰지 않는다

적·투사체·젬을 전부 `Dictionary` 배열로 들고 `_draw()` 한 곳에서 그립니다.
수백 개를 노드로 만들면 생성·해제 비용만으로 프레임이 무너집니다.

### 원만 겹치면 다 똑같아 보인다

처음에는 모든 것을 원으로 그렸습니다. 색만 다를 뿐 실루엣이 같아서 여섯 종류가 다 한 덩어리로
보였습니다. 지금은 종류마다 **몸통 형태 · 팔다리 길이 · 기울기**를 다르게 잡습니다.

| | 실루엣 |
|---|---|
| 좀비 | 등이 굽고 두 팔을 앞으로 늘어뜨림 |
| 질주 좀비 | 앞으로 크게 기울고 보폭이 넓음, 팔은 뒤로 |
| 비대 좀비 | 배가 크고 다리가 짧음, 몸이 좌우로 흔들림 |
| 폭탄 좀비 | 배에 코어가 빛나며 몸이 부풀었다 줄어듦 |
| 침 뱉는 좀비 | 목이 길고 목주머니가 부풂 |
| 변이체 | 어깨가 솟고 **한쪽 팔만 거대**, 뿔과 갈비뼈 |

여기에 걷기 위상(`walk`)과 바라보는 쪽(`face`)을 적마다 들려 보내 다리를 젓게 합니다.
셀 음영은 `Geometry2D` 교차 없이 **같은 도형을 빛 쪽으로 밀어 한 번 더 칠하는** 방식입니다.

### `fill_fan` 은 배칭을 끊는다 — 그림 성능의 전부

그림을 살찌우자 200마리에서 **23fps** 로 떨어졌습니다. 원인은 도형 개수가 아니라
`RenderingServer.canvas_item_add_triangle_array`(= `G2.fill_fan`) 였습니다.
호출마다 별도의 그리기 명령이 생겨 배칭이 끊깁니다. 반면 `draw_circle` · `draw_line` 은
엔진이 묶어 처리합니다.

| | 200마리 기준 |
|---|---|
| 머리·간이 실루엣을 `fill_fan` 으로 | **23 fps** |
| 같은 그림을 `draw_circle` 두 번으로 (`Art._ball`) | **60 fps** |

**둥근 것은 반드시 `draw_circle` 로 그리세요.** `fill_fan` 은 몸통처럼 원으로 못 만드는
도형에만 씁니다. 젬도 같은 이유로 한 겹으로 줄였습니다 — 회수가 밀려 100개쯤 깔리면
그것만으로 프레임이 반토막 났습니다.

화면 안 적이 **70마리를 넘으면** 팔다리·외곽선·눈을 건너뛰고 덩어리 두 개만 그립니다
(`Art.detail`). 그 상황에서는 어차피 개체를 구분해 볼 수 없습니다.

### 자동 공격은 정말로 자동이어야 한다

처음엔 기본 무기가 **이동 방향**으로만 나갔습니다. 서 있으면 아무것도 못 하는 셈이라
"공격은 자동" 이라는 장르 규칙과 어긋납니다. 지금은 **가장 가까운 적**을 향해 던집니다.

## 그림을 바꾸고 싶다면

[art/README.md](art/README.md) 에 파일 이름 · 크기 · 지켜야 할 네 가지와
**이미지 AI 에 그대로 붙여 넣을 프롬프트**를 정리해 뒀습니다.
PNG 를 넣고 `build.bat` 만 다시 돌리면 됩니다. 코드는 손댈 필요가 없습니다.

## 테스트

자동화된 테스트가 없습니다. 검증은 `build.bat` 성공 + `dist\zombie-defense.exe` 실행으로 합니다.
수치를 건드렸다면 최소 2~3분은 직접 플레이해서 레벨업 속도와 화면 밀도를 확인하세요.
